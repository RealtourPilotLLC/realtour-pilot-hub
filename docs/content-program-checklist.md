# Content Program — requirement checklist

Phase 0 verification, Sep 21 2026, against head `8e1f153` (which is exactly the commit the spec audited, and is deployed).


## Jordan's clarifications, Sep 21 2026 — these override the spec where they differ

1. **Video Pro is two separate four-hour sessions.** Book the existing four-hour product twice; no
   new Aryeo product. A month is fully scheduled only when two distinct, confirmed sessions are
   linked to that client and month. One booking still shows one session remaining.
2. **Videographer eligibility is per PRODUCT, read from Aryeo.** Harrison is not currently assigned
   to Starter, Accelerator or Pro. James is. `is_service_provider` and general availability do NOT
   establish eligibility — my Phase 0 report drew that inference and was wrong. Future assignment
   changes in Aryeo must be reflected without a deploy.
3. **No planning deadline and no 20th booking cutoff.** The 15th is a scheduling reminder and a
   Kyle follow-up milestone, not a cutoff and not a promise of delivery within the month. The
   first-month and agency-delayed-work exemptions stand.
4. **Never invent a production date.** "Not scheduled" for genuinely unbooked work; "Production
   date needs verification" plus a Kyle task when filming happened but the appointment or its end
   time is missing. An unknown deadline must never read as on time.
5. **Transcripts are Google Meet via Google Drive; Calendly identifies the booking.** Calendly
   Notetaker is not a requirement, so its 403 is not a blocker.
6. **The new preparation rule must be enforced through the real booking path**, with existing
   bookings staying stable while new requests use the corrected rule.
7. **Jordan test account:** phone 2155348650, email info@realtourpilot.com. Resolved.

Standing: client invitations and new client-facing automations stay held until Jordan tests the
Jordan account and approves rollout.

## Batches shipped

| Batch | Commit | Contents |
|---|---|---|
| Phase 0 | `ee2c0a2` | This checklist and the provider capability matrix |
| 1 | `a94d18a` | Preparation clock (48 weekday hours from call end), reminder calendar, production anchor per session |
| 2 | `b7b6738` | Per-product videographer eligibility from Aryeo, honest production dates, Pro as two confirmed sessions, Stripe activation to portal access, teammates, Jordan test account |
| 3a | `e679460` | **The drafting chain.** Neither planning path reached a script: SCRIPT_DRAFT was never enqueued by anything, and `submitInterview` set a status while the portal told the client "we'll draft the script from your answers". `contentDrafting.ts` settles both, as a sweep behind the new `script_drafting` switch. Staff see "Still owed this month" with the honest reason per topic. |
| 3b | `58c77e0` | **The client's own verdict on a script (F09).** Pinned to the version they read, so an edit after their yes brings the buttons back. Two records: the ledger timeline (CLIENT_APPROVED / CLIENT_CHANGES with the actor) and a ScriptSuggestion work item in the queue staff already open. A change request is never a model call. |
| 3c | `5e05b79` | **Per-topic planning questions (F10).** The six house templates each say "per-topic wording is the AI's job" and nothing did it. The roles, order and gap rules stay the policy's; only the sentences change, and anything thin falls back. Runs ahead of the client. |
| A07 | `35042d2` | **Proximity no longer confirms a content session.** Positive evidence required (provider id / month link / content deliverable); two candidates go AMBIGUOUS to Kyle instead of a guess. 18 checks, pure function. |
| A01–A06 | `c359ce4` | **Five audit findings of one shape** — a write that failed, recorded as if it had gone well. Revision evidence reports `known:false`; "marked sent" converges and repairs; the editor brief appends under a lock; an ambiguous send is not a failure; the SMS digest carries only complete lines; Slack task processing retries. |
| W01 | `420f4fd` `7c51a40` | **Correcting my own note.** `LISTING_CHANGED` is real and already handled better than the handler I wrote — reverted. Its `videos` array is EMPTY on the events we have, so it is not a media-added signal either. |
| 4a | `6da0549` | **F23 — the client library stops being keyed on an array position.** Re-keyed onto Aryeo's own video id where the row's URL provably matches one video: 155 of 159, 0 collisions. |
| 4b | `e6d7c9f` | **F12 — the photographer says WHICH topics they filmed**, not how many videos. 166 filming dates were derived and 0 confirmed by anyone who was there. Nothing pre-ticked; an unticked topic was not filmed; a missing appointment end time raises a verification flag rather than a date. |
| 4c | `c5c52e3` | **F18 — all three portal-profile boxes now reach the team.** Working preferences never reached the editor; none of the three ever reached the photographer. |
| 4d | `6a7baf8` | **F14 — an approved cut that never reached the client's library is now found.** Repaired 2 of 8 in 45 days; one was a real client's video, absent from their portal since Aug 31. |
| 4e | `611714c` | **A send floor under the outbox**, so a synthetic client cannot text a real handset. Narrow by design: real clients untouched. 17 checks against the live database on a read-only connection. |
| — | `(this)` | The board asks for the strategy call unless the client is visibly planning the month in writing. Blast radius measured: 3 of 31 live months, all TEST. |

### Verified on the Jordan test account (Sep 22)

Created with `create-test-client.ts --jordan --phone --scenarios`: client
`cmucrtvy100009kpoprlsryjd`, enrollment `cmucrtwco00019kpodjl6hcbe`, month 2026-09.

Walked end to end in the browser: portal home / topics / strategy / schedule /
resources / profile; the per-topic questions; **draft → approve → release →
the client presses "I'll film this" → the staff chip turns "client signed off"**;
then a change request whose exact words land on the ledger and in the suggestion
queue. Mobile (375×812) checked on the same journey. §25 acceptance:
**6 PASS, 0 FAIL, 4 NOT YET** (each NOT YET is an automation switch that is off
or a fixture nobody stood up — none is a defect).

### Still open after batch 2

- **A REOPENED job has no production clock at all.** Correct rather than invented, but not final:
  Jordan's Sep 16 question (a fixed turnaround from the ask, or Kyle sets it by hand) is still
  unanswered. 3 live reopened jobs carry no date and the card says so in words.
- **2705 Graystone Rd is pinned Jul 19** and will read overdue on the board. That is correct (the
  office wrote the date and the job is unsettled) but it looks like a bug until you know why.
- Retired AppSetting rows `portal-aryeo-slots` and `portal-aryeo-slots:v2:240` are still in the
  database, read by nothing. A deliberate cleanup, not done.

Integration tasks needing Jordan or a third party: `docs/integration-tasks.md`.

## Findings

Status key: EXISTING = already meets the requirement · PARTIAL = some of it · MISSING · MISCHARACTERIZED = the spec misread the code.

| ID | Status | Effort | What is actually there |
|---|---|---|---|
| F01 | PARTIAL | small | VERIFIED. src/lib/contentProgram.ts:21-25 — PACKAGE_RULES = Starter {2,1,2h}, Accelerator {4,1,4h}, Pro {8,2,4h}. Exactly as F01 states; no 3/5/10 assumption anywhere. Used at contentProgram.ts:60-73 (Aryeo sweep) and stripeSignups.ts activateSignup. LIVE DB (29 ContentEnrollment rows): 3 ACTIVE row |
| F02 | PARTIAL | medium | VERIFIED. src/lib/stripeSignups.ts sweepStripeSignups() pages up to 5x50 Checkout Sessions from the last 30 days, filters status==='complete' && payment_status==='paid', claims a ProgramSignup row on the unique checkoutId (atomic claim, lines 176-203), then activateSignup() finds-or-creates the Clie |
| F03 | PARTIAL | large | VERIFIED and BETTER THAN F03 IMPLIES on the mapping half. prisma/schema.prisma:3838-3856 ProgramCalendlyEventMapping keys classification on eventTypeUri with enabled defaulting to false. LIVE DB: both mappings exist, are ENABLED, and validationStatus='VALID' — 'Brand Discovery Call' -> BRAND_DISCOVE |
| F04 | MISSING | medium | VERIFIED — all four of F04's claims are accurate, and it understates one of them. (1) Three-business-day default: src/lib/programMonths.ts:38 DEFAULT_PREPARATION_WINDOW_DAYS = 3, applied at :205 earliestSessionAt = addBusinessDaysET(base, windowDays). (2) One submitted interview establishes preparat |
| F05 | MISSING | large | VERIFIED, and F05 understates the availability half. src/lib/portal.ts:409-445 companySlotDays() calls getSchedulingAvailability({days:21, limit:8}) then Aryeo.availableTimeslots({timezone:'America/New_York', interval:60, date}) — interval is HARD-CODED to 60 minutes; no package duration, no product |
| F06 | PARTIAL | large | VERIFIED accurate on cancellation, and understated in two places. Cancellation: src/lib/sessionRequests.ts:201-211 `cancelSessionRequest` sets status to CANCEL_REQUESTED when the row is CONFIRMED (plain CANCELLED otherwise), reopens Kyle's desk task, and the in-file comment at :205-207 says outright |
| F07 | PARTIAL | medium | VERIFIED accurate in every particular, plus two facts the finding does not mention. src/lib/contentInterview.ts:175-179 `submitInterview` sets status SUBMITTED/submittedAt and nothing else — no job, no draft. src/app/portal/actions.ts:433-453 `portalSubmitInterview` calls it, then `recalcProgramMont |
| F08 | PARTIAL | medium | VERIFIED the two-path description, with one anti-duplication guard the finding does not credit and one chain link that is genuinely absent. NEW PATH: src/lib/contentCallRecords.ts:645-676 `confirmSourceOnRecord` enqueues INGEST and, unless the legacy sweep already analysed that month, ANALYZE. src/l |
| F09 | PARTIAL | medium | VERIFIED accurate. Two staff steps, no client approval anywhere. Approval and release are separate: src/lib/contentScripts.ts:286 `approveScriptVersion` and :346 `releaseScriptVersion`, surfaced as src/app/content/actions.ts:1005 `approveScriptVersionAction` and :1041 `releaseScriptAction` (the latt |
| F10 | PARTIAL | large | VERIFIED accurate, and there is a larger data-shape blocker underneath it that the finding does not reach. What exists, as described: topic history events (src/lib/contentTopics.ts:30 recordTopicEvent), reject/archive with reasons and reintroduce (:198, :206, :214), refresh runs and suggestions (:28 |
| F11 | PARTIAL | medium | VERIFIED by reading + live data. `acceptFact()` (src/lib/clientFacts.ts:94-109) writes ONLY to the ClientFact row: status=ACCEPTED, aiContext=ALLOWED (never when confidential), reviewedBy/At, optional scope/category, and supersedes the conflicting accepted fact. It never writes a profile, asset, str |
| F12 | MISSING | large | VERIFIED. The upload portal collects exactly one integer and free text for the whole session. `videosFilmed` is gated at src/app/upload/actions.ts:540-548 and persisted at :718-719 onto Project; the field is rendered at src/components/upload/UploadPortal.tsx:1289-1305 with the label 'How many videos |
| F13 | MISCHARACTERIZED | small | I was asked to check this against live data, and the finding's implied gap does NOT exist. The saved routing DOES route personal branding to Kim. LIVE READ (read-only SELECT on AppSetting): key='editor_routing', value={"standardVideo":"john","premiumVideo":"john","personalBranding":"kim"}, updatedAt |
| F14 | PARTIAL | medium | VERIFIED, and the bridge is real but narrower and less reliable than 'already calls' implies. src/app/review/actions.ts:862-867 calls addApprovedCutToLibrary(submissionId) inside approveCut, best-effort inside try/catch, right after the atomic status win (:847-856). src/lib/portalLibrary.ts:104-137  |
| F15 | MISSING | medium | VERIFIED, and the hole is wider than the finding states. src/lib/postingKit.ts:88-118: resolveFinalFile picks `video.finalSubmissionId ?? video.approvedSubmissionId`. finalSubmissionId is set by src/lib/contentVideos.ts:258-265 to the last cut with `completedAt`, and completedAt is stamped by the Dr |
| F16 | PARTIAL | large | Finding is ACCURATE in all four of its claims; I verified each against the code and against live data. (1) Timestamped comments + bundled change request exist: src/components/portal/CutReview.tsx:80-115 (Save note / pin to paused moment) and src/lib/clientDecisions.ts:296-378 (requestChangesOnCut bu |
| F17 | PARTIAL | small | Finding is ACCURATE but UNDERSTATES what is shown and OVERSTATES the risk in one respect. What the client actually sees: src/components/portal/CutReview.tsx:250-271 renders a collapsible 'Version history (N)' panel, newest first, per version showing the version number, a state chip (Replaced by a ne |
| F18 | PARTIAL | medium | Finding is ACCURATE on every clause, and I verified two of them against live data. (1) Color picking + Dropbox brand uploads work in source: src/components/portal/PortalProfile.tsx:96-104 (native <input type="color"> → hex swatches) and src/app/api/portal/upload/route.ts:68-77 (ensureClientBrandFold |
| F19 | PARTIAL | medium | Finding is ACCURATE on all three clauses. (1) Portal memberships exist: ClientMembership with roles OWNER / COLLABORATOR / VIEWER (src/lib/portalAccess.ts:26-51), seat-scoped viewer resolution, one-time hashed 15-minute sign-in tokens with an atomic single-use consume (portalAccess.ts:130-149). This |
| F20 | PARTIAL | large | Finding is ACCURATE, and the second half of it is the more serious defect. (1) Policy, ledger, freshness checks and launch gates exist — VERIFIED and genuinely good. ReminderPolicy + REMINDER_DEFAULTS (src/lib/programReminders.ts:66-119); the ProgramReminder ledger written BEFORE any send with a uni |
| F21 | PARTIAL | large | ACCURATE, and understated in one material way. VERIFIED (read at 8e1f153, which is HEAD): • `/content` defaults to rows: `src/app/content/page.tsx:46` — `const view = sp.view === "cards" ? "cards" : "rows";`. Cards are reachable only via `?view=cards`. • 11 tabs exist exactly as claimed: `src/app/co |
| F22 | PARTIAL | large | ACCURATE in every particular I checked, and it is worth recording precisely how small two of the three fixes are. VERIFIED: • Navigation separates the areas as claimed: `src/components/portal/PortalPage.tsx:49-57` — home, videos, topics, strategy, schedule, resources (+ profile, terms off-nav). • Mo |
| F23 | MISSING | large | ACCURATE on both claims, and live data makes it far more serious than the wording suggests. This is the highest-risk finding of the four. CLAIM 1 — legacy videos keyed by listing index. VERIFIED: `src/lib/portalLibrary.ts:47` and `:58` upsert on `aryeo:${p.aryeoListingId}:${n}` where `n` is the arra |
| F24 | PARTIAL | medium | MISCHARACTERIZED IN ONE DIRECTION, ACCURATE IN ANOTHER, and the resources half is much emptier in production than "have an authoring/publishing model" implies. POSITIVE CLAIMS — ALL VERIFIED: • Transcript preferred, script fallback, honest disclosure: `src/lib/postingKit.ts:338-348` picks `transcrip |
| F25 | PARTIAL | medium | BOTH halves of the finding's description are accurate, but the required actions are only half-done. Read-only access model (accurate): src/lib/portal.ts:100 `accessFor` = status==="ACTIVE" ? FULL : READ_ONLY, applied on BOTH the token path (portal.ts:157-165) and the signed-in path (portal.ts:186-19 |
| F26 | MISSING | large | VERIFIED: nothing in the tree is a shared client<->staff program conversation. The portal has no Messages surface at all — src/components/portal/PortalPage.tsx:46-58 defines PortalTab as home/videos/topics/strategy/schedule/resources/profile/terms and MAIN_TABS lists six; the only client->staff text |
| F27 | PARTIAL | medium | The finding is factually correct on every clause, but its practical impact is smaller than it reads and I can now quantify it. CORRECT — the 7/10 rule already exists: src/lib/turnaround.ts:220 TIERS.monthly_social = targetDays 7, dueDays 10, businessDays true, with monthlyBusinessDays overridable fr |
| F28 | PARTIAL | medium | The finding is accurate and the safeguards are real, but they are narrower than "test-account tools exist" suggests. CONFIRMED: src/lib/testClients.ts:18-19 `isTestClientName` = /\btest\b/\bjohn doe\b/i, so a client named exactly "Jordan Spackman" is NOT a test record — a "Jordan Spackman TEST" name |

## Provider capability matrix

### aryeo-booking

**Verified:**

- KEY IDENTITY AND SCOPE. GET https://api.aryeo.com/v1/me -> 200. The key (len 47, prefix 163055...) is Jordan's personal access token: user 203177aa-8916-4c67-b8b8-97547100faaf, info@realtourpilot.com. His company-team-member record 3352bfaf-a2a9-4cfc-9395-df12ea3c0737 carries 34 permissions INCLUDING APPOINTMENTS_MANAGE and ORDERS_MANAGE. Group = a34b1908-d279-475e-8ec3-a7d8b9a459aa 'Realtour Pilot, LLC', timezone America/New_York, allow_order_cancellation: true, order_page_url https://media.realtourpilot.com/order.
- PRODUCT IDS AND DURATIONS - RESOLVED (GET /products, 47 products, 1 page). All three monthly-content products exist, are active:true, and sit in product category 'Monthly Social Content' 019c343c-3f92-70a1-87f3-cbf6a1ec4590: (1) 'Video Starter - 2HR Session' product 019c343b-ebd6-7139-8509-130eb1f339a5, single variant 019c343b-ebe2-711e-8b2a-58929768c1e2, duration 120, price 0; (2) 'Video Accelerator - 4HR Session' product 019c343d-27b9-72db-90a8-e941aaed31df, variant 019c343d-27d4-7346-9c5c-7ccb53241fd2, duration 240, price 0; (3) 'VIDEO PRO - 8HR Session' product 019c343f-a23b-72b0-8dee-67514390b9b5, variant 019c343f-a25e-715f-93b4-2c54561c811b, duration 240 (i.e. ONE four-hour session, not 480), price 0.
- GET /products/{id} returns 404 for every id tried (all four, incl. the three above). The Aryeo.product(id) helper in src/lib/integrations/aryeo.ts is a dead route; products must be read from the /products list.
- AVAILABILITY - DURATION IS SUPPORTED. GET /scheduling/available-dates, company-wide, 2026-09-23..2026-10-21, timezone America/New_York, interval 30: with no duration -> 25 available days, first 2026-09-23, meta.duration=0; with duration=240 -> 24 available days, first 2026-09-24, meta.duration=240. The API echoes duration in meta and the result set genuinely changes.
- AVAILABILITY - PER-CREATIVE IS SUPPORTED, AND THE ARYEO DOCS ARE WRONG ABOUT THE ID TYPE. /scheduling/available-dates and /available-timeslots document filter[user_ids][] as 'The IDs of users'. Passing James's USER id 019cf893-ecda-7139-ab8f-68e158128776 returns 422 {"filter.user_ids.0":["The selected filter.user_ids.0 is invalid."]}. Passing his COMPANY_TEAM_MEMBER id 019cf893-edf5-7017-85a6-1ec040407ab0 returns 200 with meta.user_ids echoing that same TM id. The parameter takes company-team-member ids.
- AVAILABILITY - THERE IS NO PRODUCT PARAMETER. The only accepted scoping inputs are duration, interval, timezone, filter[start_at]/filter[end_at] or filter[timeframe], filter[user_ids][] and filter[appointment_id]. Product is expressible only as a duration, or implicitly via filter[appointment_id] on an existing appointment - which I verified works: filter[appointment_id]=01a0a0ad-8dd0-71f5-b2af-9d5d9d822858 -> 200, 4 days, and meta.company_team_member_ids narrowed to the assigned creative (James).
- ELIGIBLE CREATIVES - MEASURED, NOT ASSUMED. The real eligibility flag is is_service_provider on the company team member. true: Sarah Anne (01988af0-9d23-7024-909c-e78b278adc90, user status INACTIVE), Matthew Bertsch (a860071c-0803-479e-9893-322636f746d2), James Livingston (019cf893-edf5-7017-85a6-1ec040407ab0), Jordan Spackman (3352bfaf-...), Harrison Wells (019470c9-4713-73cb-9d69-4c9bae5da500). false: Kim Miguel, Kyle Smith. The availability endpoint's default meta.user_ids is exactly those 5 TMs. Bookable days for a 240-minute session in the next 28 days: Sarah Anne 24, James 23 (first 2026-09-25), HARRISON 23 (first 2026-09-25), Matthew 0, Jordan 0, Kim 0, Kyle 0. Slots on Mon 2026-09-28 (dur 240, interval 30): Harrison 12 (first 13:00Z), Sarah Anne 12, James 4 (first 12:00Z).
- WEEKENDS ARE OFFERED BY ARYEO. Company-wide timeslots, duration 240, interval 30: Sat 2026-09-26 -> 14 slots, Sun 2026-09-27 -> 0, Mon 2026-09-28 -> 14. The spec's 'no weekend shoots in this program' is a hub-side rule; the provider will not enforce it.
- ADDRESS OWNERSHIP - RESOLVED, AND READBACK IS POSSIBLE. On the live Accelerator order #1639 (Ashley Brunner), order.address.id, listing.address.id are the SAME record: 01a0a0ae-b44f-71c6-b487-a1c99610e889. GET /addresses/01a0a0ae-b44f-71c6-b487-a1c99610e889 -> 200 with the full Address (lat/lng, postal_code 17368, city Wrightsville, state PA, timezone America/New_York, unparsed_address '[No address provided], 40.0256531,-76.5299655', is_map_dirty false). So §8's required readback is available today. The documented mutator is PATCH /addresses/{address} (street_number, street_name, unit_number, city, state_or_province, postal_code, latitude, longitude, ...). There is no documented route to repoint an existing order at a different address_id, and POST /appointments/store's address_id is explicitly 'ignored' when the order already has one - so the Address record itself is the update target.
- THE 401s IN OUR STANDING NOTES ARE ROUTE SHAPE, NOT KEY SCOPE. GET /addresses -> 401 and GET /order-items -> 401, but neither collection route exists in the API (documented: POST /addresses, GET/PATCH /addresses/{id}; POST /order-items, GET/PUT/DELETE /order-items/{id}). GET /addresses/{real id} -> 200. Likewise /discounts -> 401 while the documented GET /coupons -> 200. Also 200 with this key: /tasks, /activities, /order-forms, /customers, /customer-users, /product-categories, /territories.
- THE CONTENT PROGRAM IS ALREADY RUNNING THROUGH ARYEO, BOOKED BY HAND. Scanning the 400 most recent orders (include=items,customer,appointments,listing): 33 orders carry a Monthly Social Content line item, across 12 customers, from #1252 (2026-04-01) to #1646 (2026-09-17). Every one is Starter or Accelerator - there are ZERO 'VIDEO PRO - 8HR Session' orders, so the two-session Pro flow has never been exercised in the provider.
- LIVE DATA CONTRADICTS THE 'PACKAGE DURATION' ASSUMPTION. Appointment durations drift from the purchased package: #1637 Sarina Spinelli bought Accelerator (4HR) with a 120-minute appointment; #1519 John Collins 180; #1544 Erica Walker 210; #1411 John Collins 180; #1298 John Collins 210. #1581 (John Collins) has an appointment in status UNSCHEDULED with null start_at/end_at. #1514 (Marcee McMullen) has zero appointments.
- APPROXIMATE-AREA ADDRESSES ARE ALREADY THE HOUSE PATTERN. Real order addresses include 'June 2026 Personal Branding Content, West Chester, PA 19382', 'August 2026 Social Content, West Chester, PA 19382', 'August 2026 Personal Branding Content, Downingtown, PA 19341' and '[No address provided], 40.0256531,-76.5299655'.
- CANCELLATION / RESCHEDULE POLICY. Group allow_order_cancellation: true. Aryeo returns per-appointment permission flags, stored locally: 1,534 appointments with canCancel=true AND canReschedule=true; 39 with both false - and those 39 are exactly the 39 in status CANCELED. Aryeo imposes no lead-time rule of its own, so §8's 24-hour self-service boundary is entirely ours. Documented endpoints: PUT /appointments/{id}/reschedule, /cancel, /postpone, /schedule, /accept, /decline.
- THE HUB'S OWN RESCHEDULE/CANCEL HAS NEVER SUCCEEDED IN PRODUCTION. rescheduleAppointmentAction writes an Activity 'Appointment rescheduled to ...' (SYSTEM) and cancelAppointmentAction writes 'Appointment cancelled' (FLAG). Querying Activity for either string returns exactly ONE row, and it is an inbound Aryeo notification text about Order #1395 from 2026-06-23, not our write. Meanwhile 513 local appointments carry rescheduledAt - all set in Aryeo's own UI.
- SUBMISSION-TIME CONFLICT RECHECK IS AVAILABLE. GET /appointments/01a0a0ad-8dd0-71f5-b2af-9d5d9d822858/availability?assignee_id=019cf893-edf5-7017-85a6-1ec040407ab0&duration=240 -> 200 {"has_conflicts":false}, against a real appointment. This is the endpoint §8's 'revalidate the slot at submission' needs.
- A FULLY CONFIGURED, PROVIDER-SUPPORTED BOOKING FLOW ALREADY EXISTS. GET /order-forms -> 6 forms, one of which is 'Monthly Social Media Content', id 019c3436-a9a3-700a-bf73-093676fd606b, url https://media.realtourpilot.com/order-forms/019c3436-a9a3-700a-bf73-093676fd606b, is_public: false. Settings: availability_style DATETIME_PICKER, slot_interval_minutes 60, use_instant_appointment_scheduling true, allow_asap_scheduling true, show_user_names true, use_automated_user_assignment false, use_territory_awareness true, require_upfront_payment false. step1 Address, described as 'If you have a chosen filming location, add the address. If a filming location has not yet been decided, please just enter an approximate location. For example - "West Chester, PA" or "Bethlehem, PA"'. step2 product selection with allow_multiple_quantities true. step3 Scheduling with two custom fields - 'Content Session Notes' (uuid ac83c636-d40b-40b4-99fd-a3e10fe602b8) and required radio 'Have You Scheduled Your Strategy Call?' (uuid 12ae08a6-ae64-4442-abbc-dcd54a423823; Yes / No, I still need to schedule it. / N/A). step5 Confirm with require_tos. Those same two custom fields appear on live order #1639, so this form is the one producing real content orders.
- POST /order-form-sessions is the documented prefill hook for that form: order_form_id (required), address_id or inline address_data, customer_id / customer_group_id / customer_data, coupon_ids, step_visibility{show_address_step, show_customer_step}, success_url -> 201 ORDER_FORM_SESSION.
- companySlotDays() MEASURED AGAINST THE API - THE OVER-OFFER QUANTIFIED. Replicating its exact query (interval 60, no duration, company-wide) versus the correct Accelerator query (duration 240) and then scoped to James, slots per day: 2026-09-24 11 / 6 / 0; 09-25 11 / 8 / 2; 09-26 (Saturday) 11 / 7 / 3; 09-28 11 / 7 / 2; 09-29 11 / 7 / 6; 09-30 11 / 7 / 6. Over six days the portal as built offers 66 slots where 19 are real for a 4-hour session with James, and on Thursday 2026-09-24 it offers 11 slots on a day James has none.
- companySlotDays() IS NOT A PURE READ - it upserts AppSetting key 'portal-aryeo-slots' (src/lib/portal.ts:437-443). The live row's updatedAt is 2026-09-17T04:37:56.449Z. I did not call it; I re-issued its query directly.
- EVERY CONTENT-PROGRAM AUTOMATION IS OFF IN PRODUCTION. prisma.programAutomation.findMany() returns ZERO rows, and src/lib/programAutomation.ts states the rule 'a MISSING row is DISABLED'. That includes session_booking. Separately, prisma.programSessionRequest.findMany() returns ZERO rows - no client has ever requested a session through the hub, so driveSessionBookings() has never had a row to hand to the desk.
- ENROLLMENT BASELINE. ContentEnrollment: 29 rows - 13 ACTIVE (11 real + 2 labelled TEST: 'Bobby TEST Michael TEST', 'Cara TEST'), 3 PAUSED, 13 ENDED. The only Pro enrollment is Matthew Hutton, and it is PAUSED. ContentMonth: 110 rows. Projects with contentMonthId set: 65.
- DATA MISMATCH: Sarina Spinelli is ENDED in ContentEnrollment but has two live 2026 Aryeo content orders - #1607 (2026-08-31, Video Starter, appointment 2026-09-11) and #1637 (2026-09-14, Video Accelerator, appointment 2026-09-19).
- SAMPLE APPOINTMENT DETAIL used throughout: 01a0a0ad-8dd0-71f5-b2af-9d5d9d822858, order 01a0a0ad-8af8-73b0-8d1c-1bae9d113106 (#1639, Ashley Brunner, PAID/CONFIRMED/UNFULFILLED), start 2026-10-28T14:00:00Z, end 18:00:00Z, duration 240, status SCHEDULED, requires_confirmation false, rescheduled_at 2026-09-14T16:48:54Z, previous_start_at 2026-09-28T14:00:00Z.

**Blockers:**

- APPOINTMENT CREATION. Operation: POST /appointments/store with {order_id (required), start_at, end_at, company_team_member_ids[], item_ids[], notifyCustomer}. Verified limitation: it cannot create an appointment standalone - it requires an existing order_id, so a hub-driven booking must first create an order (POST /orders, also never exercised by this codebase), and I could not confirm the key is permitted to call either without performing a write, which Phase 0 forbids. Smallest next action, and it is not a Phase 0 action: POST /order-form-sessions against the already-configured 'Monthly Social Media Content' form (019c3436-a9a3-700a-bf73-093676fd606b) with the Jordan TEST customer and an approximate address, then complete one booking through the returned hosted flow. That exercises order + address + appointment creation in one pass, on a disposable record, via the route Aryeo actually supports and has already configured. Only if a native API create is still wanted after that: a single POST /appointments/store against that same test order.
- ADDRESS UPDATE AND READBACK. Operation: PATCH /addresses/{address_id} then GET /addresses/{address_id}. Verified limitation: the read half works (200 on the live shared Address 01a0a0ae-b44f-71c6-b487-a1c99610e889, which order #1639 and its listing both point at), and there is no documented route to repoint an order at a different address - so the Address record is the only target. The write is untested. Smallest next action: PATCH one harmless field (unit_number) on the TEST order's address and GET it back, then check whether the appointment/order titles and lat/lng followed.
- PER-CREATIVE PORTAL BOOKING CANNOT BE BUILT ON THE CURRENT AVAILABILITY CALL. Operation: offering a client real slots for their package. Verified limitation: the API has no product parameter, and the hub's companySlotDays() sends no duration, no creative and interval 60, which over six measured days offered 66 slots where only 19 fit a 4-hour Accelerator with James - including 11 on a day James had zero, and 11 on a Saturday. Smallest next action: none needed from Jordan - this is buildable now using duration + filter[user_ids][] with COMPANY_TEAM_MEMBER ids, plus GET /appointments/{id}/availability for the submission-time recheck.
- ARYEO WEBHOOKS REMAIN DEAD AND ONLY ARYEO SUPPORT CAN FIX IT (standing record, not re-probed today). Until they are re-registered, provider confirmation of any booking has to come from the hourly reconcile rather than a push. This is the one item here that needs Jordan to contact Aryeo.

**Spec corrections:**

- §3 'James is currently eligible for monthly content; Harrison becomes eligible when configured' - WRONG AS OF TODAY. Harrison Wells (TM 019470c9-4713-73cb-9d69-4c9bae5da500) is already is_service_provider:true with an active user and live bookable availability: 23 available days for a 240-minute session in the next 28, and 12 four-hour slots on Mon 2026-09-28 against James's 4. He is more available than James, not pending configuration.
- §3 'Pro always splits into two sessions of four videos/four hours ... the correct Aryeo product' - PARTLY STALE. Aryeo carries ONE product, 'VIDEO PRO - 8HR Session' (019c343f-a23b-72b0-8dee-67514390b9b5), whose single variant's duration is 240 minutes, i.e. one session. The per-session duration the spec wants is already right; the two-session split is not modelled in the catalogue, and there has never been a Pro order, so the split has to be a hub-side construct (two appointments against one order, or quantity 2).
- F01 / PACKAGE_RULES video counts (2/4/8) - CONTRADICTED BY THE PROVIDER AND BY LIVE DATA. Aryeo's own product copy says Accelerator 'Up to 5 Videos' and Pro 'Up to 10 Videos'. Three live enrollments carry videosPerMonth = 5: Marcee McMullen, Erica Walker, Bernadette Rabel. Do not let a 'reuse these values' pass silently overwrite those three; that is a real allowance change for real clients.
- §8 'The current source does not establish working Aryeo appointment creation or address updates for this account' - ACCURATE ABOUT THE SOURCE, BUT IT UNDERSTATES THE ACCOUNT, and the difference changes Phase 3. Verified today: the three product ids and their durations exist; address reads return 200; the address-owning resource is identified and shared by order and listing; PATCH /addresses/{id} is the documented mutator; GET /appointments/{id}/availability gives a real conflict recheck; and a fully configured customer-facing booking form ('Monthly Social Media Content', DATETIME_PICKER, instant scheduling, territory awareness, named creatives, the exact approximate-address wording §8 asks for) already exists and has produced 33 real content orders since April 2026. The unverified operation is narrower than 'appointment creation and address updates': it is specifically whether OUR KEY may POST, which one supervised test settles.
- §8 'Filming availability: no weekend shoots in this program' - the provider will not enforce this. Aryeo returned 14 four-hour slots on Saturday 2026-09-26 (and 0 on Sunday). The weekend exclusion must be a hub-side filter.
- F05 - CONFIRMED AND WORSE THAN STATED. companySlotDays() is company-wide with no duration and no creative, exactly as described; measured, it offers 66 slots over six days where 19 are real for a 4-hour Accelerator with James, offers 11 on a Thursday James has none, and offers Saturdays. One addition the finding misses: it is not a pure read - it upserts AppSetting 'portal-aryeo-slots', so it must not be called from a read-only context.
- F06 - CONFIRMED AND SHARPENABLE. 'their presence does not prove account permissions' is right, and I can now say the wrappers have never successfully run: zero Activity rows match the strings those actions write, across all time, while 513 appointments were rescheduled inside Aryeo's own UI. They are also staff-only (project page AppointmentManager); no portal path reaches Aryeo cancel or reschedule.
- OUR OWN STANDING MEMORY IS WRONG: reference-aryeo-api records '/discounts, /addresses, /order-items -> 401 (needs broader key scopes)'. There is no GET collection route for any of those three in the API. GET /addresses/{id} returns 200 with the current key, and GET /coupons (the real discounts read) returns 200. No scope widening is needed for address work, and the 'low value - orders already carry address' note should be replaced with 'the Address record is the update target for §8'.
- ARYEO'S PUBLISHED DOCS ARE WRONG about /scheduling/available-dates and /available-timeslots: filter[user_ids][] is documented as 'The IDs of users' but rejects user ids with 422 and requires company-team-member ids. Worth recording so the next agent does not lose an hour to it.
- §19's 'eligible creative setup' is answerable: eligibility is the is_service_provider flag on the company team member (true for Sarah Anne, Matthew Bertsch, James, Jordan, Harrison; false for Kim and Kyle). But it is not sufficient on its own - Sarah Anne's Aryeo user status is 'inactive' and she still returns 24 bookable days, so any picker must also require company_user.status === 'active'.
- DATA ISSUE FOR MIGRATION COUNTS: Sarina Spinelli is ENDED in ContentEnrollment yet has two live 2026 Aryeo content orders (#1607 Aug 31 and #1637 Sep 14 with a Sep 19 appointment). Do not treat hub enrollment status as authoritative when reconciling who is actually on the program.
- SCOPE NOTE ON REAL PRODUCTION STATE, which the spec deliberately makes no claim about and which matters for 'do not disturb active production': the content program is NOT greenfield in the provider. 33 Aryeo orders across 12 customers from 2026-04-01 to 2026-09-17 carry monthly-content line items, all booked by hand. Appointment durations already drift from the purchased package (a 4HR Accelerator with a 120-minute appointment, three others at 180/210), one order has an UNSCHEDULED null-start appointment and one has no appointment at all. Any Phase 3 reconciler that assumes appointment duration equals package duration will mislabel real jobs.

**Recommendation:** "Phase 3 should be built on the Aryeo order form, not on a hand-rolled slot picker plus a blind POST. 'Monthly Social Media Content' (019c3436-a9a3-700a-bf73-093676fd606b) already does every hard thing §8 asks for - approximate-area addresses in the exact words Jordan wants, product selection with multiple quantities for Pro, a datetime picker with instant scheduling, named creatives, territory awareness, the two content-program custom fields, and a TOS confirm step - and it is provably the form producing today's real orders. POST /order-form-sessions lets the hub prefill the customer and address and hide those steps, so the client sees only scheduling, and success_url gives us the return hook. That converts the headline blocker from 'can we create appointments' into one supervised booking test.\n\nBuild the availability layer correctly regardless, because that part needs no permission Jordan has to grant: send duration (120 for Starter, 240 for Accelerator and for each Pro session) and filter[user_ids][] with COMPANY_TEAM_MEMBER ids, filter to is_service_provider && company_user.status === 'active', exclude Saturdays ourselves, and re-check with GET /appointments/{id}/availability?assignee_id=&duration= before confirming. Retire companySlotDays() for the program path rather than extending it - it also writes a cache, so it cannot be reused inside a read-only helper.\n\nBefore Phase 3 code lands, get two writes in front of Jordan in one sitting, against a disposable TEST record: one booking through the order-form session, and one PATCH /addresses/{id} followed by a GET readback. Those two results settle every remaining provider unknown. Until they pass, keep session_booking off - it already is, along with every other ProgramAutomation switch, because the table is empty - and keep driveSessionBookings() honest rather than optimistic.\n\nTwo things need Jordan personally: asking Aryeo support to re-register the webhook subscriptions (nothing else can), and a decision on the Accelerator/Pro video counts, where Aryeo's catalogue says 5 and 10, three live enrollments say 5, and the spec says 4 and 8. Also worth his eye: Sarah Anne is bookable in Aryeo with an inactive user account, and Sarina Spinelli is ENDED in the hub while holding live Aryeo content orders.\n\nProbe scripts are LEFT IN PLACE, not deleted, because they are reusable for Phase 3 verification: /Users/jordanspackman/Realtour Pilot POT Dashboard/scripts/_recon/aryeo-key.ts (read-only key loader plus a raw GET helper) and aryeo-probe1.ts through aryeo-probe16.ts. Every one is GET-only against Aryeo and SELECT-only against Postgres; none imports src/lib/integrations/aryeo.ts or src/lib/portal.ts, precisely to avoid the cache-rebuild write class. No source file was edited, nothing was committed, and no production row was written. Aryeo reference docs were saved to the session scratchpad at .../scratchpad/aryeo-recon/ (aryeo-api.md plus the per-endpoint pages for appointment store/update/availability/reschedule/cancel, scheduling dates and timeslots, addresses patch, orders post, order-form-sessions)."

### stripe-calendly

**Verified:**

- STRIPE CREDENTIAL — the key on file is a STANDARD live secret key, not a restricted read key. `Connection[provider=stripe]` is CONNECTED, label "Stripe · Realtour Pilot LLC", lastSyncedAt 2026-09-21T08:00:12Z; the decrypted secret starts `sk_live_` and is 107 chars (restricted keys are `rk_live_`). This contradicts the header comment in /Users/jordanspackman/Realtour Pilot POT Dashboard/src/lib/integrations/stripe.ts ("Jordan pastes a RESTRICTED read key on /connections").
- STRIPE PERMISSIONS — every read probe returned HTTP 200: /v1/balance, /v1/account, /v1/products, /v1/prices, /v1/checkout/sessions, /v1/subscriptions, /v1/customers, /v1/events, /v1/webhook_endpoints, /v1/payment_links. Event read and Webhook-endpoint read are both available, so we are NOT limited to polling by permission.
- STRIPE WEBHOOKS — GET /v1/webhook_endpoints returns count=0. Zero endpoints are registered on the live account, and the repo has no receiver: /Users/jordanspackman/Realtour Pilot POT Dashboard/src/app/api/webhooks/ contains only aryeo, openphone, scripting, slack. Today the only signup path is polling: sweepStripeSignups() runs hourly from /api/cron/sync (vercel.json `0 * * * *`) and on demand from src/app/content/actions.ts:42.
- STRIPE CATALOG — 25 products live, 10 match the program regex. Verified price map: Video Starter — Month-to-Month prod_Uo3ZTc1vJ6yPVL / price_1ToRRzRrlUAkQjeVchi3zx8W $1299 recurring-month; Video Starter — 1-Year Commitment prod_Uo3aI6oez5oALB / price_1ToRTeRrlUAkQjeVRfr0qRRP $1199 recurring-month; Video Starter — Pay in Full prod_Uo3bEXrvXoIoJY / price_1ToRUYRrlUAkQjeVoYdQ9qOH $12230 one_time; Video Accelerator — Month-to-Month prod_Uo3cd97rfqJbSC with TWO active prices price_1U2ue2RrlUAkQjeVSGwsZXpB $1599 and price_1ToRVeRrlUAkQjeVGVfJSGkR $1699; Video Accelerator — 1-Year Commitment prod_Uo3dmrwsPqyCVR / price_1ToRWNRrlUAkQjeVnlqXzQZp $1499 recurring-month; Video Accelerator — Pay in Full prod_Uo3elia44pf2bh / price_1ToRX1RrlUAkQjeV9rvA1022 $15290 one_time; Video Pro — Month-to-Month prod_Uo3fPtSRRQuBSQ / price_1ToRY0RrlUAkQjeV3jXBZ0xX $3500 recurring-month; Video Pro — 1-Year Commitment prod_Uo3g19F9RKbLsK / price_1ToRYmRrlUAkQjeVJ9knIJEp $3097 recurring-month; Video Pro — Pay in Full prod_Uo3gUkXW1IPXy2 / price_1ToRZSRrlUAkQjeV0ejRCNCB $30846 one_time; TRIAL alias = "Video Accelerator - 4 videos - Give It a Try!" prod_UyZ1unQbiCOlhv / price_1TysDTRrlUAkQjeV3D19DPFJ $997 one_time (plus inactive price_1TybtiRrlUAkQjeV9X3eOAH0 $1000).
- STRIPE SIGNUP HISTORY — over the last 400 days: 104 checkout sessions fetched, 6 paid+complete, exactly 3 of them program products: 2026-09-03 Mike Flatley (Accelerator 1-Year, price_1ToRWN…, sub_1UBjYs…), 2026-08-26 Kristin Ciarmella (Accelerator M2M, price_1U2ue2…), 2026-08-25 Arielle Roemer (Starter M2M, price_1ToRRz…). No Video Pro sale, no Pay-in-Full sale, no trial checkout has ever run through this path. All 3 stored priceIds resolve to live ACTIVE prices.
- F02 CONFIRMED WITH DATA — activation creates no portal access. All three verified Stripe buyers have 0 ClientMembership rows. Mike Flatley, the only enrollment with packageSource="website", has NO portalToken at all. Across the program: 13 of 29 ContentEnrollments have a portalToken, and the whole ClientUser table holds 3 rows (2 ACTIVE, 1 INVITED). No welcome email is sent anywhere in activateSignup().
- F01 CONFIRMED — PACKAGE_RULES in src/lib/contentProgram.ts:21 reads Starter 2/1/2h, Accelerator 4/1/4h, Pro 8/2/4h, matching the spec table exactly.
- EXISTING MANUAL ALLOWANCES (spec §19) — 3 of 29 enrollments carry videosPerMonth=5 on an Accelerator whose rule is 4: Marcee McMullen (PAID_IN_FULL $15588/12), Erica Walker (MONTHLY_CONTRACT $1499/12), Bernadette Rabel (PAID_IN_FULL $15588/12). All three are packageSource="manual". These are the overrides any Stripe/Aryeo resync must not flatten.
- ONE RECORDED PACKAGE CONFLICT, ALREADY RESOLVED BY HAND — the Arielle Roemer signup row still carries note "Paid for Starter but the enrollment is set to Accelerator — left as-is, check the settings card", but her enrollment now reads Starter / 2 videos / 1 session / 2h. The note is stale text on the signup row, not an open mismatch.
- CALENDLY CREDENTIAL AND SCOPES — Connection[provider=calendly] CONNECTED, "Calendly · Jordan Spackman", lastSynced 2026-09-21T20:01:55Z. The PAT is a JWT for user 0592eae5-34a7-477b-a6f2-a5544fb1691e (info@realtourpilot.com, org cab21ef9-b922-4acc-8cab-ccdab5c1a100, tz America/New_York), issued 2026-08-24, with no exp. Its own scope claim is: availability:read availability:write event_types:read event_types:write locations:read routing_forms:read shares:write scheduled_events:read scheduled_events:write scheduling_links:write groups:read organizations:read organizations:write users:read contacts:read contacts:write meeting_recaps:read meeting_recaps:write activity_log:read data_compliance:write outgoing_communications:read webhooks:read webhooks:write. The token is NOT read-only.
- CALENDLY EVENT TYPES — 6 active, all public (secret=false): 15-Min Discovery 92c40a70-9481-455a-8190-9e17d5eefd13; 30 Minute Strategy Call 521830b5-64f2-4450-b079-2265acfcd8b2; Bio Film Discovery Call 42e3fdd7-22fa-4a8b-97b2-c186ce879588; Brand Discovery Call c1f17817-f37b-41f3-bd4c-689ba83f1962 (60min); Content Program - Strategy Call 62b064b4-4d28-469b-b1c1-8f2ac65eee4b (30min); Discovery Call 2b5c09e3-85cc-4f91-948b-1edb9930b289.
- CALENDLY MAPPINGS ARE ALREADY CONFIGURED IN PRODUCTION — ProgramCalendlyEventMapping holds exactly 2 rows, both enabled=true and validationStatus=VALID, validated 2026-09-18: BRAND_DISCOVERY → https://api.calendly.com/event_types/c1f17817-f37b-41f3-bd4c-689ba83f1962 → https://calendly.com/realtourpilot-info/brand-discovery-call; MONTHLY_STRATEGY → https://api.calendly.com/event_types/62b064b4-4d28-469b-b1c1-8f2ac65eee4b → https://calendly.com/realtourpilot-info/content-program-strategy-call. Both public URLs return HTTP 200. The BRAND_DISCOVERY row is byte-for-byte the website link named in spec §4.2.
- THE NAME-MATCHING HAZARD IS QUANTIFIED — in the window 120 days back / 60 days forward there are 55 bookings on the account: 37 on "30 Minute Strategy Call" (unmapped, generic), 16 on "Discovery Call" (unmapped, generic), 2 on "Content Program - Strategy Call", 0 on "Brand Discovery Call". A classifier matching the words "strategy" or "discovery" would have ingested 53 unrelated business calls.
- PORTAL-ORIGIN ASSOCIATION IS POSSIBLE BUT UNUSED — live invitee payloads carry `tracking` = {utm_campaign, utm_source, utm_medium, utm_content, utm_term, salesforce_uuid} plus routing_form_submission, scheduling_method, invitee_scheduled_by and questions_and_answers. On both real Content Program bookings every tracking value is null. Nothing in the hub reads them: no occurrence of tracking/utm_/salesforce_uuid/scheduling_links in src/lib/integrations/calendly.ts, src/lib/contentCallRecords.ts, src/lib/contentCalls.ts or src/app/portal, and ProgramCallRecord (prisma/schema.prisma:3863) has no origin/source/bookingToken field.
- THE PORTAL BOOKS WITH A RAW GENERIC LINK — STRATEGY_CALL_BOOKING_URL (src/lib/integrations/calendly.ts:23) is rendered as a plain <a href> with no prefill, no UTM and no month/enrollment parameter at src/components/portal/PortalPage.tsx:165 and :344, src/components/portal/tabs/ScheduleTab.tsx:79 and :86, src/components/portal/PortalScheduler.tsx:176 and :189, src/components/portal/tabs/HomeTab.tsx:128 and :133, and in reminder emails at src/lib/programReminders.ts:677. This is precisely the arrangement spec §8 rules insufficient.
- IDENTITY MATCHING ALREADY FAILS ON REAL DATA — 3 ProgramCallRecord rows exist, all MONTHLY_STRATEGY: 2026-08-07 Erica Walker MATCHED ("matched via backupEmail", transcript CONFIRMED); 2026-09-09 info+bobbytest@realtourpilot.com CONFIRMED_BY_STAFF (an internal §25 acceptance fixture); 2026-09-11 Arielle Roemer UNMATCHED_INVITEE with no client, no enrollment and no month, because Calendly carried arielle.roemer@foxroach.com while her Client row is arielleroemer@gmail.com with backupEmail null. That is 1 of the 2 genuine client bookings unrouted.
- THE ALIAS ESCAPE HATCH EXISTS BUT IS UNCOMPLETED — ClientEmailAlias holds exactly 1 row: arielle.roemer@foxroach.com → client cmt7msalo000b9kg1kuk666hf, active=true, source=calendly, verifiedAt=NULL. resolveInviteeIdentity only accepts aliases with verifiedAt non-null, so the proposal sits inert and the call stays unmatched.
- THE DISCOVERY PATH HAS NEVER RUN — 0 ProgramCallRecord rows of type BRAND_DISCOVERY, 0 bookings on the Brand Discovery Call event type in the last 120 days, and 0 rows in ProgramOnboarding. Discovery → onboarding → foundational strategy is entirely unexercised in production.
- CALENDLY NOTETAKER IS OFF — GET /meeting_recaps returns 403 "Meeting recap access is not available. Required features are not enabled for your account". The comment in src/lib/integrations/calendly.ts is accurate. Consequence: the Drive sweep is the only transcript source, and ProgramTranscriptSource is 9 drive/UNMATCHED to 1 drive/CONFIRMED.
- CALENDLY WEBHOOKS ARE AVAILABLE ON THIS PLAN — GET /webhook_subscriptions?scope=user returns 1 live subscription (Calendly-for-Slack, callback https://calendly-for-slack.calendly.com/webhooks/calendly/d5b60baa-7f09-407e-aac4-54e42d016f77, created 2025-05-29); org scope returns 0. The endpoint is readable, so invitee.created / invitee.canceled subscriptions are not plan-blocked.
- NO STRIPE-PRICE-TO-ARYEO-PRODUCT MAPPING EXISTS AT ALL — grep across src/ finds no Stripe price id, no Stripe product id and no Aryeo product identifier. Package is derived solely from the Stripe product NAME by PROGRAM_PRODUCT_RE in src/lib/stripeSignups.ts:82, and src/lib/sessionRequests.ts carries no product/service selection for booking.
- hub.realtourpilot.com returns HTTP 307 (login redirect) — reachable and serving.

**Blockers:**

- Calendly Notetaker transcripts. Operation: GET https://api.calendly.com/meeting_recaps. Verified limitation: HTTP 403 "Meeting recap access is not available. Required features are not enabled for your account." Smallest next action: Jordan enables Meeting Notetaker in Calendly account settings (it may require a plan tier change) and tells me; the code already degrades to {skipped} and will start working with no change. Until then Google Drive/Meet is the sole transcript source and it currently sits at 9 UNMATCHED to 1 CONFIRMED.
- Stripe verified event handling. Operation: POST /v1/webhook_endpoints. Verified limitation: none technical — GET /v1/webhook_endpoints returns 200 with count=0 and the key is a full-access sk_live_. The blocker is authorisation: registering an endpoint writes to Jordan's live Stripe account. Smallest next action: Jordan creates one endpoint at https://hub.realtourpilot.com/api/webhooks/stripe for checkout.session.completed, customer.subscription.updated and customer.subscription.deleted, and pastes the whsec_ signing secret into Vercel as STRIPE_WEBHOOK_SECRET; I build the receiver and keep the hourly sweep as reconciliation. I can perform the creation myself the moment he says so.
- Server-verifiable portal booking origin. Operation: POST /scheduling_links (single-use link) and/or one parameterised test booking to prove the UTM round-trip. Verified limitation: none technical — scheduling_links:write and scheduled_events:write are both in the token's scope claim, and the invitee tracking fields are readable. The blocker is that both are writes to Calendly and one of them creates a real calendar booking. Smallest next action: Jordan's go-ahead for a single throwaway booking on the Content Program strategy link carrying ?utm_source=portal&utm_content=<monthId>&salesforce_uuid=<enrollment token>, which I then read back and cancel.
- Credential posture decision. The hub stores a full-access sk_live_ key while src/lib/integrations/stripe.ts documents a restricted read key. Jordan chooses: keep full access (needed to self-manage the webhook endpoint) or rotate to an rk_live_ restricted key, which would then need read permissions on Checkout Sessions, Products, Prices, Subscriptions, Customers, Balance and Balance Transactions plus write on Webhook Endpoints. This is his call, not mine, and nothing else in Phase 0 is blocked by it.

**Spec corrections:**

- §19 asks for "Exact Calendly event-type mapping for discovery/monthly calls" and F03 implies it still needs establishing. It is already resolved in production and has been since 2026-09-18: ProgramCalendlyEventMapping holds both rows, enabled and VALID, with BRAND_DISCOVERY = c1f17817-f37b-41f3-bd4c-689ba83f1962 and MONTHLY_STRATEGY = 62b064b4-4d28-469b-b1c1-8f2ac65eee4b. The classifier already refuses to fall back to names (purposeForEventType reads the table and nothing else). What remains of F03 is only the portal-origin half.
- F02 says to "inspect available Stripe permissions first" and implies polling may be forced. It is not. The key on file is a standard sk_live_ (107 chars), every read endpoint returns 200 including /v1/events and /v1/webhook_endpoints, and there are simply zero endpoints registered. Verified event handling is a decision and one POST away, not a permission problem. Correspondingly, the comment in src/lib/stripeSignups.ts calling this "the hub's restricted read key" and the header of src/lib/integrations/stripe.ts saying "Jordan pastes a RESTRICTED read key" are both factually wrong about the credential actually stored, and should be corrected when that file is next touched.
- F01's required action "Add verified Stripe-price-to-Aryeo-product mappings" is accurate that nothing exists, but understates the difficulty: the mapping cannot be keyed on product name, because prod_Uo3cd97rfqJbSC ("Video Accelerator — Month-to-Month") carries TWO active prices, $1599 and $1699, and a live enrollment sits on each. Any mapping must be price-id-keyed.
- F03's caution "Do not assume an email match proves a subsequent call came through the portal" is right, and the state is worse than the spec describes: invitee email is the ONLY matcher today, and it already fails on real production data — Arielle Roemer's genuine 2026-09-11 Content Program strategy call is sitting UNMATCHED_INVITEE with no client and no month because Calendly captured her brokerage address. The auto-proposed ClientEmailAlias exists but verifiedAt is NULL, so the proposed-alias safety valve has never actually been closed by a human.
- §2 correctly notes the review never queried the live database, which left several claims uncountable. The real numbers for this area: 29 ContentEnrollments (14 ACTIVE, 3 PAUSED, 12 ENDED), 3 ProgramSignups (all ACTIVATED), 3 ProgramCallRecords, 0 ProgramOnboarding rows, 13 of 29 enrollments with a portal token, 3 ClientUser rows in total, 0 ClientMembership rows for any of the three paying website buyers.
- §4.2's statement that the website redirects payers to calendly.com/realtourpilot-info/brand-discovery-call is consistent with a real, mapped, publicly reachable event type — but that exception path has never been used: 0 bookings on Brand Discovery Call in the last 120 days and 0 BRAND_DISCOVERY call records ever. Treat every discovery-side assertion in the spec as design intent, not observed behaviour.
- The seven source-characterization checks in §2 make no claim about Stripe or Calendly runtime configuration, and none of my findings contradict them. But the spec's framing that "source availability does not establish that a feature is enabled" cuts both ways here: for Calendly the configuration is MORE complete than the source review could see, while for Stripe the welcome/access half is exactly as absent as F02 says.

**Recommendation:** "Where the implementation should start in this area, in priority order.\n\n1. Stripe access-on-payment (F02) is the only fully unblocked, fully verified gap and should be built first. activateSignup() already finds/creates the Client, creates the ContentEnrollment with the right rules, and rings the owner bell; it stops short of a ClientUser + ClientMembership + portalToken + welcome email. Extend that one function rather than adding a parallel onboarding path, and dedupe the welcome on ProgramSignup.checkoutId, which is already the unique claim key. Evidence that this is genuinely missing and not merely unobserved: three paying buyers, zero memberships, and the one true website enrollment has no portal token.\n\n2. Add a price-id catalog table rather than extending the name regex. Key it on the ten verified price ids above, carry package, billingType, billingMonths and the eventual Aryeo product/duration, and fall back to the existing name parser only for an unrecognised price (parking the signup NEEDS_REVIEW as it does today). This kills the $1599-vs-$1699 ambiguity and gives §19 its \"verified Stripe-price-to-Aryeo-product mapping\" anchor point. Note that no Video Pro or Pay-in-Full checkout has ever occurred, so those rows will be first-exercised by the Jordan test account, not by history.\n\n3. Keep polling and ADD the webhook — do not replace one with the other. The spec's §4.1 rule is satisfied either way because both read Stripe's own ledger; the webhook only buys latency. Build /api/webhooks/stripe with signature verification and have it call the same activateSignup path, so sweepStripeSignups stays as the reconciliation net for a missed or replayed delivery. Hold the endpoint creation for Jordan's explicit go-ahead since it writes to his live account.\n\n4. For Calendly, do NOT rebuild classification — it is already correct and already configured. Build only the portal-origin layer. My recommendation is belt-and-braces and cheap: (a) mint a single-use scheduling link per enrollment/month via POST /scheduling_links, and (b) append utm_source=portal, utm_content=<monthId> and salesforce_uuid=<opaque enrollment nonce> to the booking URL. Store the nonce, then on ingest require that invitee.tracking.salesforce_uuid matches a stored nonce before treating the booking as portal-originated. That needs one new ProgramCallRecord column (bookingOrigin plus the nonce) and a read of a field the API already returns. Bookings without a nonce keep the current behaviour: email identity, and Kyle/Jordan approval to connect — which is exactly the spec's \"outside bookings\" rule, with the website Brand Discovery type as the standing exception.\n\n5. Close the alias loop before anything else touches identity. One real client call is stuck right now on an unverified alias. Surface ClientEmailAlias proposals as a one-click confirm on the call-review task; that single action would convert Arielle's 2026-09-11 call from UNMATCHED_INVITEE to MATCHED and is a genuine before/after acceptance case for the implementation phases.\n\n6. Plan the Jordan test account to avoid provider side effects on both providers. Stripe: do not create a test checkout on the live account — seed the ProgramSignup row directly, or use a Stripe test-mode key, and note that a live-mode key cannot see test-mode sessions. Calendly: a test booking is a real calendar event on Jordan's real calendar; use the strategy-call link, cancel it immediately afterwards via scheduled_events:write, and keep the invitee address distinct from any real client (the existing info+bobbytest@realtourpilot.com fixture pattern already works and is correctly flagged CONFIRMED_BY_STAFF).\n\n7. Two documentation fixes to fold into the first commit that touches these files: correct the \"restricted read key\" comments in src/lib/integrations/stripe.ts and src/lib/stripeSignups.ts, and correct the \"Read-only: the hub never books or cancels\" header in src/lib/integrations/calendly.ts to say that this is a deliberate policy, not a token limitation — the token holds scheduling_links:write, scheduled_events:write and webhooks:write.\n\nProbes left in place, all read-only, all untracked: /Users/jordanspackman/Realtour Pilot POT Dashboard/scripts/_recon/sc-connections.ts, sc-stripe.ts, sc-calendly.ts, sc-access.ts, sc-callmatch.ts. I did not edit any source file, did not touch prisma/schema.prisma, ran no git write commands, and made no provider call other than GET."

### config-and-people

**Verified:**

- ALL 13 CONTENT-PROGRAM AUTOMATION SWITCHES ARE OFF, AND NOT ONE HAS EVER BEEN LEFT CONFIGURED. Raw SQL `select key, enabled, "enabledAt", "disabledAt", "lastRunAt" from "ProgramAutomation"` returned 0 rows (confirmed twice: Prisma findMany and $queryRawUnsafe). Per src/lib/programAutomation.ts the rule is 'a MISSING row is DISABLED', so reminders, transcript_jobs, ai_runs, publishing, script_share_email, portal_invites, portal_login_email, topic_refresh, strategy_generation, session_booking, cut_transcripts, caption_assistant and fact_extraction all read OFF right now. The /settings panel will say 'never configured' for every one.
- THE SWITCH HISTORY HAS BEEN DESTROYED, AND THE SPEC'S PICTURE OF 'NEVER TURNED ON' IS INCOMPLETE. The ProgramReminder ledger holds 7 rows, all 2026-09-17, and two of them carry requestedBy='reminders-cron' (ESCALATION + BOOK_SESSION, outcome=accepted, real Gmail id 1a0afe0b73f9dde7). sendReminderNow() and evaluateReminders() both refuse to act unless isAutomationEnabled('reminders') is true, so the row demonstrably existed and was enabled on Sep 17. `grep -rn 'programAutomation.delete'` over src/ and scripts/ finds NO delete path, and setAutomation() only ever upserts. So the rows were removed out-of-band and enabledBy/enabledAt/disabledBy/disabledAt are gone.
- F13 IS WRONG — CORRECT IT. The saved AppSetting `editor_routing` reads exactly {"standardVideo":"john","premiumVideo":"john","personalBranding":"kim"}, updatedAt 2026-08-25T01:12:48.999Z. Personal-branding editing IS already routed to Kim in production. The spec's claim ('both source defaults specify manual assignment, not Kim. Actual saved routing was not read') is true of the code default (DEFAULT_ROUTING.personalBranding = null) but false of production: settings.ts editorRouting() overlays the stored row, and 'kim' is in ROUTABLE_EDITORS so the retired-key guard does not reject it. F13's required action is already satisfied at the settings layer; only the program-specific handoff needs wiring.
- CLIENT-FACING EMAIL WORKS, FROM info@ ONLY. src/lib/outbox.ts realOutboxProvider() hard-codes mailbox 'info@realtourpilot.com' for every channel=='email' send. Live Google tokeninfo (read-only GET) on that mailbox returns scopes 'email calendar.events drive.readonly gmail.readonly gmail.send userinfo.email openid' -> gmail.send GRANTED. End-to-end evidence: 4 OutboxMessage email rows, all state=accepted with real Gmail message ids (1a0afc31dd17b466, 1a0afc36988c3c51, 1a0afe0b73f9dde7, 1a0afe0c3765e306), latest 2026-09-17T14:58:56Z, to info+caratest@ and info+bobbytest@realtourpilot.com. No reconnect is needed for the content program's email rail.
- KYLE'S MAILBOX IS DEAD AND NOTHING IS FLAGGING IT. The stored refresh token for hello@realtourpilot.com returns HTTP 400 {"error":"invalid_grant"} from https://oauth2.googleapis.com/token. The hub can neither read nor send as hello@. Meanwhile the Connection row still reads provider=gmail, status=CONNECTED, accountLabel='Gmail · info@realtourpilot.com, hello@realtourpilot.com', lastSyncedAt 2026-09-18T21:02:00Z; and BOTH gmail-reconnect SmartTasks are COMPLETED (gmail-reconnect-hello@realtourpilot.com closed 2026-08-31T21:43:25Z). gmailSendHealth() reports canSend=null for hello@, which the code treats as 'couldn't check', not 'broken', so no alert fires.
- THE SLACK BOT CANNOT LIST CHANNELS, AND HAS NO im:write. auth.test (read-only) on the stored xoxb token: ok=true, team='Realtour Pilot', user='realtour_pilot_bot', and the x-oauth-scopes header is exactly 'chat:write,chat:write.public,channels:history,groups:history,im:history,mpim:history'. channels:read/groups:read are absent, so slackChannels() throws SlackError: missing_scope (reproduced). im:write is absent, so the conversations.open fallback inside slackDmUserDetailed() cannot succeed — the file's own comment names this exact signature.
- EVERY INTERNAL OPS ALERT LANDS IN KYLE'S DM ALONE. `vercel env ls production` lists 18 variables and SLACK_ALERT_CHANNEL is not among them; it is also absent from the local .env. With the env unset and slackChannels() failing on missing_scope, alertDestination() (src/lib/notify.ts) falls through its catch to kyleSlackId() -> TeamMember 'Kyle Smith'.slackId = U07SCBTPDC7. §13's requirement that automation exceptions be 'visible to Jordan, Kyle and James' is not met by the Slack rail as configured.
- THE EDITORS DO HAVE SLACK IDS — the editors.ts comment saying to 'fill once we know their Slack ids' is stale. TeamMember rows: Kim Miguel slackId=U0ASP9C1WRK, phone +639153944459, MANAGER, active; John Mark slackId=U0BQHV69STV, phone NULL, VA, active. notify.ts resolves the DM from TeamMember.slackId (not from EDITORS.slackUserId), so Slack reaches both. John has no phone, so any SMS fallback silently no-ops for him. Saved notify-prefs for both: mention/project_message/job_ping = {slack:true, sms:false}; review_ready and shoot_change = {slack:false, sms:false}. A brand-change ping must be emitted under a kind that maps to one of the first three, or it reaches neither of them.
- THE FIVE PEOPLE, THEIR REAL ROWS, AND WHAT THEY CAN ACTUALLY REACH (computed by running the shipped canAccess() against the stored AppUser rows). Jordan Spackman info@realtourpilot.com — AppUser OWNER/ACTIVE, no per-user overrides, TeamMember role PHOTOGRAPHER, phone +12155348650, slackId U07D2KJH1JP, opsAlerts=true; all pages. Kyle Smith hello@realtourpilot.com — AppUser ADMIN/ACTIVE, permissions null, TeamMember MANAGER, phone ends 4889, slackId U07SCBTPDC7, opsAlerts=true; content=Y settings=Y editing=Y review=Y upload=Y tasks=Y clients=Y users=Y connections=n. James Livingston james@realtourpilot.com — AppUser ADMIN/ACTIVE, permissions {"mypay":true}, TeamMember PHOTOGRAPHER + creativeManager=true + isServiceProvider=true, slackId U0AM2Q0M17W; identical page set to Kyle. Kim Miguel kim@realtourpilot.com — AppUser EDITOR/ACTIVE, editorKey='kim', password set, last login 2026-09-15T20:05:09Z; reaches ONLY editing/resources/training/assistant (content=n review=n upload=n tasks=n settings=n). John Mark john@realtourpilot.com — EDITOR/ACTIVE, editorKey='john', same four pages. Harrison Wells harrisonwells457@gmail.com — PHOTOGRAPHER/ACTIVE, last login 2026-09-21T20:11:57Z (today); shoot/mypay/upload/resources/training/assistant/review/editing, content=n.
- §3'S WARNING ABOUT JAMES IS ANSWERED: his ADMIN role DOES grant the screens. James reaches /content, /editing, /review, /upload, /tasks, /clients, /users and /settings today. He does NOT reach /connections (ownerOnly) and he cannot flip an automation switch or edit the reminder policy — programActions.setAutomationAction() and every write in settings/reminderActions.ts call requireOwner(). Kim, the person §3 makes responsible for personal-branding editing, cannot reach /content at all, so anything routed to her must surface on /editing or /edit/<id>.
- THE DUTY OWNERS ARE REAL AND RESOLVE TO ACTIVE USERS. ProgramOwnerAssignment holds exactly 6 live DEFAULT rows: STRATEGY, SCRIPTS and ESCALATION -> Jordan Spackman (OWNER/ACTIVE); SCHEDULING, DELIVERY and REMINDERS -> Kyle Smith (ADMIN/ACTIVE). No ENROLLMENT- or MONTH-scoped override exists.
- THERE IS NO DUTY FOR JAMES. OWNER_DUTIES has six values and none of them is production/editing. §3 assigns James 'Production/editing exceptions and creative-team coordination' and §13 makes him the recovery owner for upload/edit exceptions, but the owner model has no slot that can name him, so a production exception today has no assignee in this system.
- JORDAN'S VERIFIED CONTACT DETAILS, AND §16 IS RIGHT ABOUT THE PHONE NUMBER. Staff-verified: info@realtourpilot.com (AppUser OWNER + TeamMember) and +1 215 534 8650 (TeamMember.phone). The (215) 645-4889 in the spec is KYLE'S — confirmed twice over: TeamMember 'Kyle Smith'.phone ends 4889, and Connection provider=openphone has accountLabel 'OpenPhone · (215) 645-4889', i.e. it is the company line, not Jordan's test contact.
- TWO OTHER 'Jordan Spackman' RECORDS EXIST AND NEITHER IS A SAFE TEST CONTACT. Client cmqikskt1008u9k9qej9ltjy5 — jspackman215@gmail.com, +12678279038, aryeoCustomerId 2ee9574f-9b8c-4f04-a23d-75fcf46b9660, created 2026-06-17. Client cmtl9n46u0001la04skvn78nb — info@realtorpilot.com (note the typo domain: realt-O-rpilot), +1267-827-9038, aryeoCustomerId 1ef935bc-9f89-48ca-b03d-7343ee52c966, created 2026-09-03. The existing 'Bobby TEST Michael TEST' client also carries +12678279038. Both are real Aryeo customers; neither passes isStaffControlledEmail().
- F28 IS CORRECT AS WRITTEN. src/lib/testClients.ts: isTestClientName = /\btest\b|\bjohn doe\b/i, so 'Jordan Spackman' alone is NOT a test record and 'Jordan Spackman TEST' is. isStaffControlledEmail = /^[^@\s]+@realtourpilot\.com$/i, which accepts plus-tags (info+jordantest@realtourpilot.com passes) and rejects every gmail address. The existing synthetic portal people all follow that pattern: info+caratest@, info+caraviewtest@, info+caracollabtest@, info+bobbytest@realtourpilot.com.
- THE SIX LAUNCH GATES ACTUALLY IN FORCE, EACH CONFIRMED. G1 — 0 ProgramAutomation rows, so all 13 switches read off. G2 — portal_invites off: portalAccess.ts restricts seat creation to TEST clients on staff-controlled addresses and sends no email. G3 — portal_login_email off: a sign-in link is never emailed and mintLoginLink is TEST-only; AppSetting 'portal-login-requested:cmu4ta0on00039kxi2nfka4bg' (2026-09-17) is a recorded-but-not-sent request, i.e. the gate working. G4 — ReminderPolicy.testClientsOnly defaults true, suppressing every non-TEST enrollment even if the switch were flipped. G5 — AUTH_ENFORCE is set in Vercel production and enforcing: https://hub.realtourpilot.com/ and /content both return 307 to /login. G6 — zero real portal people: 3 ClientUser rows, all info+…test@realtourpilot.com (Cara TEST ACTIVE, Viewer TEST ACTIVE, Collab TEST INVITED), and 3 ClientMemberships (2 live, 1 revoked) all on client cmu4ta0a900009kxi0eqnsln2.
- THE GATES DO NOT COVER THE EXISTING CLIENT SMS SWEEPS, AND THOSE ARE RUNNING TODAY. There is NO `auto_texts` AppSetting row, so autoTextRules() runs entirely on DEFAULT_AUTO_TEXTS: enabled:true, Mon–Fri 09:00–16:30 ET, confirmation 48h before shoot, delivery feedback-ask with requireMonthlyBatch:true, after-hours auto-reply enabled, welcome text enabled. settings.ts states plainly that program reminders 'never read, written or reused' these. Evidence they are live: 50 OutboxMessage sms rows state=accepted, latest 2026-09-21T19:43:17Z, plus auto-confirm-*/auto-delivery-*/auto-welcome-* AppSetting markers running through 2026-09-21. The only related saved row is `text_templates` = {"confirmation":"","deliveryAll":"","deliveryPartial":""} (all blank, so built-in wording applies).
- THE CALENDLY EVENT-TYPE MAPPING §5 ASKS FOR ALREADY EXISTS, IS VALIDATED AND IS FRESH. ProgramCalendlyEventMapping holds exactly 2 rows, both enabled=true, validationStatus=VALID, lastSyncedAt 2026-09-21T20:01:53Z (well inside the 6h staleSchedulerSyncHours): BRAND_DISCOVERY -> event type c1f17817-f37b-41f3-bd4c-689ba83f1962 at https://calendly.com/realtourpilot-info/brand-discovery-call (the exact URL §4.2 names), and MONTHLY_STRATEGY -> 62b064b4-4d28-469b-b1c1-8f2ac65eee4b at https://calendly.com/realtourpilot-info/content-program-strategy-call. Host for both is https://api.calendly.com/users/0592eae5-34a7-477b-a6f2-a5544fb1691e. Connection provider=calendly is CONNECTED, lastSynced 2026-09-21T20:01:55Z. There is no `content_call_rules` AppSetting row, so contentCallRecords.ts code defaults apply on top.
- A LIVE INSTANCE OF §4.4'S IDENTITY CONFLICT ALREADY EXISTS. ProgramCallRecord has 3 rows: 1 MATCHED, 1 CONFIRMED_BY_STAFF (Bobby TEST), and 1 UNMATCHED_INVITEE with enrollmentId and clientId both NULL — Arielle Roemer booked the MONTHLY_STRATEGY call as arielle.roemer@foxroach.com while her ContentEnrollment sits on arielleroemer@gmail.com. By contrast Erica Walker's record DID link (clientId cmqikslcr00999k9q5809i927) despite booking as erica@parealtycafe.com against an enrollment on erica@luxelocalrealestate.com.
- THE PRODUCTION ENROLLMENT PICTURE. 29 ContentEnrollment rows: 13 ACTIVE, 3 PAUSED, 13 ENDED. Package mix among ACTIVE: 2 Starter (Mike Ciunci, Arielle Roemer), 11 Accelerator, 0 Pro. THE ONLY Pro enrollment is PAUSED (Matthew Hutton), so there is no live client to exercise two-session behaviour against. THREE ACTIVE 'Accelerator' rows carry videosPerMonth=5 rather than 4 — Bernadette Rabel, Marcee McMullen and Erica Walker — which are exactly the manual overrides §3 says must survive Stripe/Aryeo sync. packageSource values seen are 'manual' (most), 'aryeo' (4) and 'website' (1, Mike Flatley), a third value the schema comment ('aryeo | manual') does not mention.
- ProgramSignup holds 3 rows; ProgramOnboarding holds 0. The onboarding table §4 and §15 depend on is completely empty.
- THE CLIENT PORTAL SIGN-IN URL §2 COULD NOT RETRIEVE IS LIVE: https://hub.realtourpilot.com/portal/login returns 200 with <title>Sign in — RealTour Pilot</title>. /portal with no token returns 404. The magic-link landing route is /portal/auth/<token> (portalAccess.ts loginUrl()).
- OPEN WORK BY ASSIGNEE RIGHT NOW (SmartTask, status OPEN or IN_PROGRESS): kyle 43 OPEN, jordan 33 OPEN, kim 10 OPEN + 1 IN_PROGRESS, john 2 OPEN, external_agency 3 OPEN + 1 IN_PROGRESS, and 24 OPEN with no assignedKey at all.
- PROVIDER CONNECTION STATUS (Connection table, secrets not read). CONNECTED with a present secret: ai, aryeo (lastSynced 2026-09-21T20:31:41Z), aryeo_webhook, calendly, dropbox, epidemic_sound, frameio, frameio_app, gmail, openphone, openphone_webhook, plaid (PNC), quickbooks (realm 9341453504443108), slack, slack_user, stripe, topaz (569 credits). Every row has lastError NULL. The aryeo row's metadata records webhooks registered 2026-09-02T01:40:00Z to https://hub.realtourpilot.com/api/webhooks/aryeo with "signed":false across 10 event types.

**Blockers:**

- hello@realtourpilot.com cannot send or receive through the hub. Operation: OAuth refresh for that mailbox. Verified limitation: POST https://oauth2.googleapis.com/token with the stored refresh token returns HTTP 400 {"error":"invalid_grant"}. Work completed: info@ confirmed healthy with gmail.send, so nothing in the content program's own email rail is blocked. Smallest next action: Jordan opens /connections -> Google -> Reconnect and approves BOTH mailboxes; only he can, because /connections is ownerOnly. Note that both gmail-reconnect tasks are already COMPLETED, so nothing will prompt him without this being raised.
- The Slack app cannot list channels and cannot open a first DM. Operation: conversations.list and conversations.open. Verified limitation: auth.test returns granted scopes 'chat:write,chat:write.public,channels:history,groups:history,im:history,mpim:history' — channels:read, groups:read and im:write are all absent, and slackChannels() reproducibly throws missing_scope. Consequence: every internal ops alert silently routes to Kyle's DM alone, and a first-ever DM to an editor will fail. Work completed: editor Slack ids confirmed present on the TeamMember rows, so no data is missing — only scope. Smallest next action: EITHER Jordan reinstalls the Slack app with channels:read, groups:read and im:write, OR (no reinstall needed) set SLACK_ALERT_CHANNEL in Vercel production to the ops channel id, which bypasses discovery entirely. The permalink cache shows channel C09SL8A1E1X already in use by the bot and would be the candidate to confirm.
- There is nothing to configure for videographer eligibility. Operation: restrict a monthly-content session to an eligible creative. Verified limitation: no eligibility field, setting or Aryeo parameter exists in the tree — companySlotDays() requests company-wide availability with no provider, product or duration. Work completed: confirmed James and Harrison both carry isServiceProvider=true and active TeamMember rows, so the people exist. Smallest next action: this is a Phase 3 build item, not a settings flip; it should be designed against whatever Aryeo actually supports for provider-scoped availability, which the provider-capability recon needs to establish first.

**Spec corrections:**

- F13 is wrong on the point that matters. Spec: 'Personal-branding routing is configurable, but both source defaults specify manual assignment, not Kim. Actual saved routing was not read.' Read it: AppSetting editor_routing = {"standardVideo":"john","premiumVideo":"john","personalBranding":"kim"}, saved 2026-08-25T01:12:48.999Z. Production already routes personal branding to Kim; the code default the spec describes is overridden by the stored overlay in settings.ts editorRouting(), and 'kim' survives the retired-key guard because it is in ROUTABLE_EDITORS. Keep the F13 action about the program-specific handoff and drop the claim that routing points at manual.
- §2's 'The public login URL could not be retrieved during this pass' is now resolved: the client portal sign-in page is live at https://hub.realtourpilot.com/portal/login (HTTP 200, <title>Sign in — RealTour Pilot</title>), and the magic-link landing route is /portal/auth/<token>.
- §2's 'The team uses the hub; clients have not been invited' is correct but understates one path. No real client has a ClientUser or ClientMembership — all 3 of each are synthetic TEST records. However 13 of the 29 ContentEnrollment rows carry a legacy `portalToken`, an unguessable share link that is NOT gated by the portal_invites switch. Those are a pre-existing client-facing access route that the launch gates do not cover, and §16's migration plan should account for them rather than assume the switches are the only door.
- §5 and F03 read as though the Calendly event-type mapping still has to be established. It is already built, populated and healthy: two enabled ProgramCalendlyEventMapping rows, both validationStatus=VALID, both lastSyncedAt 2026-09-21T20:01:53Z, covering BRAND_DISCOVERY (the exact brand-discovery-call URL §4.2 cites) and MONTHLY_STRATEGY. What remains is the portal-origin association and the outside-booking approval path, not the mapping itself.
- §2's summary of the launch gates as the ProgramAutomation switches is incomplete in a way that could bite. The existing automated CLIENT TEXT sweeps are live and entirely outside those gates: there is no `auto_texts` AppSetting row, so DEFAULT_AUTO_TEXTS applies (enabled:true, confirmation, delivery feedback-ask, after-hours reply and welcome text all on), and 50 sms outbox rows are accepted with the latest at 2026-09-21T19:43:17Z. A content-program client with an Aryeo booking or delivery WILL be texted today regardless of every content switch being off.
- §3's owners table names James for 'Production/editing exceptions and creative-team coordination', and §13 makes him a recovery owner, but the owner model has no duty that can hold him: OWNER_DUTIES is STRATEGY, SCRIPTS, SCHEDULING, DELIVERY, ESCALATION, REMINDERS, and all six live DEFAULT rows point at Jordan or Kyle. Either a production duty is added or §3 should say explicitly that James is reached through task assignment rather than duty ownership.
- §16's list of reference-client candidates should be re-ranked against what is actually in the database. Sarina Spinelli's enrollment is ENDED. Erica is ambiguous — there are two: Erica Walker (ACTIVE, Accelerator with a manual videosPerMonth=5 override, has a portalToken and a linked MONTHLY_STRATEGY call record) and Erica Wright (ENDED). Bernadette Rabel is ACTIVE with the same 5-video override and a saved portal-prefill row. Also worth knowing before choosing: there is no ACTIVE Pro enrollment anywhere (the only Pro, Matthew Hutton, is PAUSED), so no real client can exercise two-session behaviour and the Jordan test account must supply it.

**Recommendation:** "WHAT I DID AND DID NOT TOUCH. Nothing was written. No source file was edited, nothing was committed, no message, email or text was sent, no booking or charge was made. Every provider call was a GET or an auth/tokeninfo read. The 13 modified files in `git status` are the other agents' comms-coaching work and I left them alone. I deliberately avoided two transitive-write traps in my own area — programOwners.ownersFor() mints ProgramOwnerAssignment rows on first call, and portal.ts companySlotDays() writes an AppSetting slot cache — and read the underlying tables instead. Phase 1 probes should do the same.\n\nMY PROBES ARE LEFT IN PLACE, not deleted, because Phase 1 will want to re-run them: /Users/jordanspackman/Realtour Pilot POT Dashboard/scripts/_recon/config-people.ts (automation switches, all 416 AppSetting keys, TeamMember/AppUser/Connection rows, outbox, portal people, TEST clients), config-people2.ts (reminder ledger, enrollments, Jordan's contact records, gmail send health, Slack destination), config-people3.ts (raw-SQL switch check, Slack token scopes, per-person page access via the shipped canAccess), config-people4.ts (per-mailbox Google token detail, ProgramOwnerAssignment read), config-people5.ts (Calendly mappings, call records, task assignment). All are read-only; config-people4.ts performs a Google token refresh, which is a provider POST but persists nothing.\n\nTHE FOUR THINGS THAT SHOULD CHANGE THE PLAN.\n\n1. Reminders were switched on and ran for real on Sep 17, then the ProgramAutomation rows vanished with no delete path in the codebase. Treat 'all off' as true today but 'never configured' as false. Before Phase 1 touches anything, the owner should set each of the 13 switches deliberately — even setting them OFF creates the row and restores disabledBy/disabledAt, so the Settings panel stops lying about the program's history.\n\n2. Two internal notification rails are broken and neither is visible. hello@ returns invalid_grant, so Kyle's mailbox is dark, while its reconnect task sits COMPLETED and the Connection still reads CONNECTED. And the Slack bot lacks channels:read/groups:read/im:write, so channel discovery fails and every ops alert funnels into Kyle's DM. §13 wants exceptions visible to Jordan, Kyle AND James; as configured, that is one DM to one person. Setting SLACK_ALERT_CHANNEL in Vercel is the cheap half of the fix and needs no Slack reinstall.\n\n3. The content program's launch gates are sound — six of them, each verified, and portalAccess.ts enforces them in the server path rather than the UI — but they do not cover the existing client SMS sweeps, which are running on code defaults with no saved row and sent as recently as today. Any real client enrolled in the program is already reachable by confirmation, delivery-feedback and welcome texts through the Aryeo path. That should be stated explicitly to Jordan before he is asked to authorise rollout, because 'client-facing automations are held' is only true of the new ones.\n\n4. For the Jordan test account, use info+jordantest@realtourpilot.com and name the client 'Jordan Spackman TEST'. That combination satisfies both existing guards without weakening either: isTestClientName needs the word TEST, and isStaffControlledEmail accepts plus-tags on realtourpilot.com. It also matches the pattern the four existing synthetic portal people already use. Do NOT reuse either of the two real 'Jordan Spackman' Client rows — both are live Aryeo customers with real customer ids, one sits on a typo domain (info@realtorpilot.com), and the +1 267 827 9038 on both is a number I could not verify against any staff record. If Jordan wants a real phone on the test account, ask him to confirm that number rather than inferring it from the old Bobby TEST record that shares it.\n\nONE MORE THING WORTH A SENTENCE TO JORDAN: Lauren Spackman holds a second ACTIVE OWNER login with a password set. Because requireOwner() is the only guard on the automation switches and the reminder policy, that account can turn on client invitations and reminders. If that is not intended, it is a one-field change on /users before rollout."

### data-and-reference-client

**Verified:**

- INVENTORY (all read-only SELECTs against the live Neon DB via scripts/_recon/cp-inventory.ts, run 2026-09-21T20:33Z). ContentEnrollment 29 (13 ACTIVE, 3 PAUSED, 13 ENDED; 13 have a portalToken; 23 packageSource=manual; 0 have callMode set). ProgramSignup 3. ContentMonth 110 (80 historical=true / status IMPORTED, 30 OPEN). ContentTopic 962. ContentScript 164. ContentScriptVersion 174. ContentScriptRelease 16. ContentStrategy (legacy) 6. ContentStrategyVersion 7. ContentStrategyProposal 1. ContentPillar 4. ContentTopicSelection 15. ContentTopicEvent 49. ContentTopicSuggestion 50. ContentTopicRefreshRun 14. ContentInterview 4. ContentInterviewAnswer 17. ContentVideo 168. ContentVideoSource 179. ClientDecision 2. ContentCutTranscript 0. ContentScriptMatch 0. ContentCaptionDraft 8. ProgramSessionRequest 0. ProgramCallRecord 3. ProgramTranscriptSource 10. ProgramTranscriptJob 1. ProgramOnboarding 0. ProgramCalendlyEventMapping 2. ClientAsset 2. ClientAssetVersion 3. ClientFact 815. ClientUser 3. ClientMembership 3. PortalVisit 2165. PortalComment 5. PortalVideo 163. ProgramReminder 8. ProgramAutomation 0. ProgramOwnerAssignment 6. ProgramEnrollmentChange 35. ContentImportBatch 4. ContentImportItem 6. PortalResource 0. AgentProfile 26. ContentNote 804.
- PACKAGE ALLOWANCES vs F01's PACKAGE_RULES: 26 of 29 enrollments match (Starter 2v/1s/2h x4, Accelerator 4v/1s/4h x21, Pro 8v/2s/4h x1). EXACTLY 3 differ, all Accelerator stored as 5 videos/month with packageSource=manual: Bernadette Rabel, Marcee McMullen, Erica Walker. These are live manual allowance overrides that §3 says must survive Stripe/Aryeo reconciliation.
- REFERENCE-CLIENT SCORING (scripts/_recon/cp-candidates.ts + cp-timeline.ts). Columns = months / topics / scripts / approved-strategy-versions / call evidence / ContentVideos / review cuts (max round). Erica Walker (ACTIVE, Accelerator 5v): 13 months, 215 topics, 52 scripts, 0 strategy versions, 1 ProgramCallRecord + 10 months with pasted transcripts, 36 videos, 1 cut (max round 1). Marcee McMullen: 8 / 116 / 24 / 1 APPROVED / 6 month-transcripts / 23 videos / 0 cuts. Joe Sutow: 5 / 140 / 12 / 1 APPROVED / 3 month-transcripts / 14 videos / 2 cuts (max round 1). John Collins: 7 / 67 / 18 / 1 APPROVED / 5 month-transcripts / 14 videos / 0 cuts. Bernadette Rabel: 8 / 86 / 15 / 1 APPROVED / 6 month-transcripts / 10 videos / 0 cuts. Sarina Spinelli (ENDED): 2 / 26 / 5 / 0 strategy / 1 month-transcript / 2 videos / 3 cuts with max round 2 — the ONLY client in production with a real revision round. Mike Ciunci: 14 / 29 / 0 scripts / 0 strategy / 32 videos / 5 cuts. Alex Ercole Stackhouse (PAUSED): 12 / 24 / 10 / 0 strategy / 19 videos / 0 cuts, and no portalToken at all.
- ONLY FOUR real clients have an approved strategy, and all four are identical in shape: Bernadette Rabel, Marcee McMullen, Joe Sutow, John Collins each hold exactly one ContentStrategyVersion v1, status APPROVED, sourceKind=import, approvedAt 2026-08-24, releasedAt NULL, structureTemplate S1 (Joe's is 'unknown'), 3 sections, rawText 4.2k–7.6k chars, both enrollmentId and strategyId populated. Erica Walker, Sarina Spinelli, Mike Ciunci and Alex Ercole Stackhouse have NO strategy version. The 6 legacy ContentStrategy rows are the parents of these same 7 versions (4 real + Cara TEST + Bobby TEST) — not a competing set.
- SCRIPT APPROVAL HAS NEVER RUN FOR A REAL CLIENT. Of 164 ContentScript rows: all 164 have currentVersionId, 154 have topicId, 164 have monthId, releaseState is historical=144 / released=6 / withheld=2 / null=12. Only 8 have approvedVersionId and only 6 have sharedVersionId — and every one of those 8/6 belongs to Cara TEST or Bobby TEST Michael TEST. Zero real-client scripts are clientVisible. So every 'approved' real script (139 rows with status APPROVED) is an IMPORTED historical record, not something Jordan approved in the hub.
- F23 CONFIRMED AND UNDERSTATED — filmed dates. Of 168 ContentVideo rows, 165 have filmedAt and ALL 165 equal their project's shootDate to the millisecond; 0 differ; 3 are null. There is not one photographer-confirmed filming date in production. src/lib/contentVideos.ts:198 sets `const filmedAt = p.shootDate && p.shootDate < new Date() ? p.shootDate : null` once per project and stamps it onto every video of that project (lines 224, 269, 296, 302). The spec's 'derives SOME filmed dates from a past project shoot date' is actually 100% of them.
- F23 CONFIRMED — Aryeo listing-index keying. src/lib/portalLibrary.ts:41-60 upserts on `externalKey = aryeo:<listingId>:<n>` where n is the position in `media.videos.filter(v => v.playback || v.download)` — a FILTERED array index, so a file losing its playback URL shifts every later file's key. 159 of 163 PortalVideo rows are index-keyed this way, spread over 42 distinct Aryeo listings, of which 40 hold more than one file; 157 of the 159 sit in a multi-file listing and would be silently repointed (title/thumb/playback/download rewritten in place by the `update` branch) if Aryeo reorders or drops a file. The same file also carries a third date fallback: `deliveredAt: p.deliveredAt ?? p.shootDate ?? new Date()`.
- F23's other index risk is currently DORMANT, not live. contentVideos.ts step 2 pairs Aryeo files to review-cut videos `byIndex` only when `aryeoRows.length === cutVideos.length`. Measured in production: 0 projects satisfy that condition (review-cut videos and Aryeo files live on disjoint project sets), so 0 files are presently exposed to positional merging. The externalKey risk above is the real one.
- TOPIC/SCRIPT -> VIDEO LINKAGE IS ENTIRELY ABSENT. Of 168 ContentVideo rows: topicId set on 0, selectionId on 0, scriptId on 1, scriptVersionId on 1, deliverableId on 4, outputId (the DeliverableOutput per-video identity) on 0, mappedBy (staff correction) on 0. From the other side, 0 of 164 ContentScript rows have videoId. The chain in §15 (Topic selection -> Script/version -> Logical video) is broken at exactly one joint: scripts ARE linked to topics and months (154/164 and 164/164), and videos ARE linked to projects and months (168/168 both), but nothing joins script/topic to video.
- CLIENT APPROVAL EVIDENCE IS EFFECTIVELY ZERO. ClientDecision = 2 rows, both on Cara TEST. Of 168 ContentVideos, 160 are DELIVERED but only 1 has approvedSubmissionId and only 1 has finalSubmissionId; 5 have releasedToClientAt. Of 16 review submissions on content projects, 2 have completedAt and 1 has clientApprovedDecisionId. PortalComment = 5 rows total. So F15's 'exempt verified legacy delivered content' exemption set is essentially the whole delivered library (160 videos / 163 PortalVideo rows).
- PREVIOUS CONTENT vs CURRENT. ContentMonth: 95 months key before 2026-09, 14 are 2026-09, 1 is 2026-10. ContentVideo by monthKey: 162 rows fall before 2026-09 (Previous Content) and 6 are 2026-09 (1 DELIVERED, 4 EDITING/CLIENT_REVIEW, 1 other). Zero videos have a filmedAt whose month differs from their monthKey, so no display-vs-allowance month conflict exists today (A42) — but that is only because filmedAt IS the project shoot date, so the two can never disagree. Earliest month with videos is 2025-04.
- QUESTIONABLE ASSOCIATIONS, counted (scripts/_recon/cp-questionable.ts). (a) 1 of 63 content projects is filed on a month its shoot does not belong to: Bernadette Rabel, month 2026-08, shootDate 2026-09-14, '1142 Hamilton St, Allentown'. (b) 10 of 55 delivered content projects carry deliveredAt <= shootDate (Jamie Achberger x5, Alex Ercole Stackhouse x3, Erica Walker x2) — the delivery stamp on those is backfill, not evidence. (c) 2 content projects have no shootDate at all. (d) 2 duplicate client+title video pairs (Mike Ciunci 'reel 1' and 'reel 2' each twice). (e) 51 of 54 DELIVERED content projects never had a review cut, so their videos exist only as Aryeo library rows. (f) videos-per-month vs ContentMonth.videosOwed: 16 months match exactly, 14 months have MORE videos than owed, 13 have FEWER, 67 have none — e.g. Erica Walker 2026-03 owed 5 got 10, Mike Ciunci 2025-12 owed 2 got 6, Erica Walker 2026-08 owed 5 got 2.
- IMPORT ARTEFACT IN ERICA WALKER'S TOPIC BANK: 139 topics sit in status SELECTED, of which only 3 have a script and 103 are stacked on the single month 2026-05 (the rest: 2025-10=4, 2026-01=6, 2026-02=6, 2026-03=5, 2026-04=5, 2026-06=6, 2026-07=4). A11 ('select then unselect without filming') has no clean baseline on this client.
- THE 7-10 BUSINESS DAY PROMISE IS INTERNAL-ONLY — it is stated to STAFF, never to clients. Definition lives in src/lib/turnaround.ts:220 `monthly_social: { label: '7–10 business days', targetDays: 7, dueDays: 10, businessDays: true }`, matched by the regexes at lines 231-233 on 'Video Starter|Accelerator|Pro' / 'monthly|social content'. Staff-facing statements: src/app/resources/guide/content.ts:542 ('Monthly content batch | 10 business days') and :912 ('Every due date is the shoot date plus that deliverable's promise … monthly content 10 business days'), plus src/app/page.tsx:242 (Kyle's Ops Day card) and src/app/pipeline/page.tsx:21. I grepped every portal component, /app/portal, /app/api/portal, and the reminder/script-share/text-sweep senders: the string '7-10', '7–10' or 'business days' in a delivery sense appears NOWHERE client-facing. The only timing promises a client can actually read are in PortalPage.tsx:73 ('at least a few business days between the call and the shoot', '48 hours' notice' to reschedule') and PlanningChoice.tsx:27 / ScheduleTab.tsx:51 ('session booking opens a few business days after your answers are in').
- WHAT THE PROMISE CLOCK MEASURES TODAY: it starts at the PROJECT SHOOT DATE and ends at PROJECT delivery, not at an appointment end and not at a client approval. src/lib/deliveryBoard.ts:228 `clockStart = p.shootDate ?? (isMonthlyContentJob(...) ? now : null)` — for a monthly job with no shoot date the anchor is a moving `now`. dueAtFor() then closes the window at 5pm ET on the 10th business day. So the current implementation promises neither 'first reviewable edit' nor 'approved downloadable file' — it promises the agency's batch delivery stamp on Project.deliveredAt. Measured against that stamp on 55 delivered content projects: median 11 calendar days, p90 38, max 154, 21 of 55 over 14 calendar days. F27 is confirmed.
- F28 CONFIRMED, with sharper evidence than the spec gives. src/lib/testClients.ts is the sole definition: `isTestClientName = /\btest\b|\bjohn doe\b/i`. 'Jordan Spackman' does not match it — and this is not hypothetical: production already holds TWO live Client rows named exactly 'Jordan Spackman' (jspackman215@gmail.com / +12678279038, and info@realtorpilot.com [note the 'realtor' typo] / +1267-827-9038). Neither is treated as synthetic by any guard.
- THE GUARD IS WIRED INTO 9 REAL CODE PATHS, which is stronger than the spec implies: portalAccess.ts (155, 183, 405 — pre-launch portal people must use @realtourpilot.com), scriptShare.ts (255, 288 — suppresses a send to a TEST client at a non-staff address), programReminders.ts (379, 528, 888), sessionRequests.ts:162 (desk tasks suppressed for TEST unless PROGRAM_DESK_TASKS_FOR_TEST=1), contentCallRecords.ts (213, 215), contentCalls.ts (418, 474), programOnboarding.ts:150, deliveryExceptions.ts:748, plus assertTestClient in scripts/create-test-client.ts and scripts/run-acceptance-scenarios.ts.
- THE EXISTING TEST FIXTURES ARE NOT FULLY ISOLATED. 'Bobby TEST Michael TEST' carries email bobmike0214@gmail.com — a real external Gmail, not staff-controlled — and phone +12678279038, which is the SAME number as the 'Jordan Spackman' client rows. The guards suppress sends to it (isStaffControlledEmail fails), so that fixture can never receive a test message; 'Cara TEST' (info+caratest@realtourpilot.com) can. Cara TEST is also the only client in the database with a released strategy (releasedAt set) and the only holder of both ClientDecision rows.
- JORDAN'S IDENTITY ROWS, read-only: AppUser 'Jordan Spackman' info@realtourpilot.com OWNER/ACTIVE, linked to TeamMember cmqidx2on00009ke2vvuyx40p; TeamMember 'Jordan Spackman' info@realtourpilot.com / +12155348650, role PHOTOGRAPHER, active. The two Client rows named 'Jordan Spackman' carry a DIFFERENT phone (+12678279038) and neither of their emails matches the AppUser. Also present: AppUser 'Lauren Spackman' jordan.laurenspackman@gmail.com OWNER/ACTIVE.
- CALENDLY IS ACTUALLY CONFIGURED IN PRODUCTION, not merely present in source (this is a positive correction to F03's 'source availability does not establish configuration'). ProgramCalendlyEventMapping holds exactly 2 rows, both enabled=true, validationStatus=VALID, validatedAt 2026-09-18, lastSyncedAt 2026-09-21T20:01Z (30 minutes before my probe): 'Brand Discovery Call' -> purpose BRAND_DISCOVERY (calendly.com/realtourpilot-info/brand-discovery-call) and 'Content Program - Strategy Call' -> purpose MONTHLY_STRATEGY (calendly.com/realtourpilot-info/content-program-strategy-call). Both created by info@realtourpilot.com, same hostUri.
- ALL THREE ProgramCallRecord ROWS, and the identity problem they show. (1) MONTHLY_STRATEGY/COMPLETED 2026-09-11 18:30-19:00, invitee arielle.roemer@foxroach.com, clientId NULL, matchState=UNMATCHED_INVITEE, no month linked — yet Arielle Roemer IS an ACTIVE Starter enrollee. A real orphaned call, and live evidence for A02/A03. (2) Bobby TEST, matchState=CONFIRMED_BY_STAFF, transcriptState=FAILED, month linked. (3) Erica Walker, 2026-08-07 16:45-17:15, matchState=MATCHED, transcriptState=CONFIRMED, targetMonthKey 2026-08, month linked, calendly URI present — but the invitee email is erica@parealtycafe.com while her Client.email is erica@luxelocalrealestate.com, so the MATCHED verdict was not reached on email equality. Note also Erica Walker and Alex Ercole Stackhouse share the luxelocalrealestate.com domain.
- ProgramAutomation has ZERO rows, and ContentEnrollment.callMode is null on all 29. Whatever §13's automation map assumes about saved switches, none are persisted yet. 63 of 110 ContentMonths carry a pasted transcriptText and all 63 have transcriptProcessedAt; only 1 month has callRecordId set (Erica's). strategyCallStatus: COMPLETED 65, NOT_SCHEDULED 42, NOT_REQUIRED 2, SKIPPED 1. preparationStatus is set on 30 months; planningMode on 0.

**Blockers:**

- Aryeo listing-video ORDER STABILITY — operation: GET /listings/{id}/media (via getListingMedia in src/lib/integrations/aryeo.ts). Verified limitation: none yet; I simply did not call it, because it is a provider call and this phase is read-only reconnaissance of the database. Work completed: established that 157 of 163 library rows depend on this ordering and named the exact upsert (portalLibrary.ts:47/58). Smallest next action: call getListingMedia twice on one multi-file listing and diff the returned video ids/order. This is a GET and creates nothing.
- JORDAN'S TEST CONTACT DETAILS — operation: creating the Jordan Spackman TEST enrollment and routing its emails/texts. Verified limitation: the database offers three conflicting candidates (info@realtourpilot.com via AppUser/TeamMember, +12155348650 via TeamMember, +12678279038 via two 'Jordan Spackman' Client rows) and §16 explicitly forbids guessing. Work completed: found every candidate row and confirmed the create-test-client guards would accept info+jordantest@realtourpilot.com. Smallest next action: Jordan confirms one email and one mobile number for TEST destinations; everything else about the test account can be built without him.
- PROVIDER-SIDE ISOLATION FOR THE TEST ACCOUNT — operation: any Aryeo booking/address write or Stripe write made by a test journey. Verified limitation: the existing guards (testClients.ts callers listed above) suppress internal desk tasks, reminders and script-share emails, but I found NO guard between a TEST client and an Aryeo appointment or address write. Work completed: enumerated all 9 guard call sites. Smallest next action: decide whether the test account uses a sandbox Aryeo credential or a mock adapter before Phase 3 books anything — this is the §16 requirement that cannot be met by the name guard alone.

**Spec corrections:**

- F23 UNDERSTATES THE FILMED-DATE PROBLEM. The spec says program video sync 'derives SOME filmed dates from a past project shoot date'. It derives ALL of them: 165 of 168 ContentVideo rows have filmedAt exactly equal to their project's shootDate and 0 differ (the other 3 are null). There is no photographer-confirmed filming date anywhere in production, so 'review old associations before exposing them' means reviewing the entire library, not a subset.
- F23's INDEX-PAIRING RISK IS SPLIT, AND ONLY HALF OF IT IS LIVE. The spec treats 'Aryeo legacy videos are keyed partly by listing index' as one issue. There are two mechanisms. (1) The PortalVideo externalKey `aryeo:<listingId>:<n>` (portalLibrary.ts:47/58) is live and affects 157 of 163 rows across the 40 multi-file listings — that one is real. (2) The positional `byIndex` merge in contentVideos.ts step 2 fires only when a project's Aryeo file count equals its review-cut video count; measured in production that is 0 projects, so it is currently dormant. Fixing (1) is urgent; (2) is a latent trap to close, not a present corruption.
- F01 IS RIGHT ABOUT THE CONSTANTS BUT INCOMPLETE ABOUT THE DATA. PACKAGE_RULES does say 2/4/8, and 26 of 29 enrollments store exactly that. But three ACTIVE Accelerator clients (Bernadette Rabel, Marcee McMullen, Erica Walker) store 5 videos/month with packageSource=manual. Any implementation that 'corrects' stored allowances to the constants would silently cut those three clients' monthly allowance by one video. §3's 'preserve manual production overrides' has three named beneficiaries, and the spec does not say so.
- F03 IS STALE ON CALENDLY CONFIGURATION, IN THE HUB'S FAVOUR. The spec (§2) says 'source availability does not establish that a feature is enabled or configured in production' and lists typed Calendly mappings as merely existing. They are configured AND healthy: 2 ProgramCalendlyEventMapping rows, enabled, validationStatus VALID, validated 2026-09-18, last synced 2026-09-21T20:01Z, mapped to purposes BRAND_DISCOVERY and MONTHLY_STRATEGY. §19's 'exact Calendly event-type mapping for discovery/monthly calls' is already answered — what is NOT answered is portal-origin association, and the one real orphan (arielle.roemer@foxroach.com, matchState UNMATCHED_INVITEE, clientId null) proves the matching gap the spec worries about.
- F28 IS CORRECT BUT UNDERSELLS THE EVIDENCE, AND THE FIX IS TRIVIAL. 'Jordan Spackman alone is not considered a test record by the current guard' is exactly right — /\btest\b|\bjohn doe\b/i does not match it — and production already contains TWO live Client rows named 'Jordan Spackman' that no guard treats as synthetic. But the spec's own required action ('create an internally labeled Jordan Spackman TEST account') resolves it by construction: that name DOES match the guard, and scripts/create-test-client.ts would accept it unchanged. The real residual gap is not the name regex; it is that no guard sits between a TEST client and an Aryeo/Stripe write.
- F14 OVERSTATES WHAT THE REVIEW-ROOM BRIDGE HAS ACTUALLY DONE. addApprovedCutToLibrary exists and is wired, as the spec says — but it has produced only 4 'sub:<id>' library rows out of 163, against 159 Aryeo rows. 51 of the 54 DELIVERED content projects never had a review cut at all. So 'reuse this bridge' is correct, but 'reliable missing-link repair' is not an edge case: it is the path for ~97% of the existing library.
- §15's REQUIRED CHAIN IS BROKEN AT ONE SPECIFIC JOINT, WHICH IS BETTER NEWS THAN THE SPEC IMPLIES. Client -> Enrollment -> Month -> Script and Topic is intact in production (164/164 scripts have monthId, 154/164 have topicId, 168/168 videos have monthId and projectId). Only the Script/Topic -> Video edge is empty (0 topicId, 0 selectionId, 1 scriptId on 168 videos; 0 videoId on 164 scripts). The migration work in §16 step 5 is one join to repair, not a rebuild of the chain.
- §19's DELIVERY-PROMISE QUESTION HAS A CLEANER ANSWER THAN 'KEEP THEM SEPARATE UNTIL VERIFIED'. There is no existing client-facing meaning to preserve. The 7–10 business-day window is stated only to staff (turnaround.ts:220, resources/guide/content.ts:542 and :912, page.tsx:242, pipeline/page.tsx:21) and never to a client anywhere in the portal or any send path. The clock as built runs shootDate -> Project.deliveredAt, so it currently promises neither the first reviewable edit nor the approved download — it promises the batch delivery stamp. Jordan is free to define the client-facing milestone without contradicting anything a client has been shown in the hub.

**Recommendation:** "REFERENCE CLIENT: use JOE SUTOW, with Erica Walker as the volume stress-test and Sarina Spinelli as the revision fixture. Joe is the only real client who is non-empty on all six axes §16 names: strategy (1 ContentStrategyVersion v1 APPROVED, sourceKind=import, 7,620 chars, 3 sections, enrollment- and strategy-linked), topics (140, spanning SAVED 78 / RECOMMENDED 40 / SELECTED 8 / SCRIPTED 4 / DELIVERED 8 / REJECTED 2 — every state the topic engine has), scripts (12, all with topicId + monthId + currentVersionId), calls (3 months carrying processed transcripts), linked videos (14 across 3 months, from BOTH aryeo and review sources — the only strategy-holding client with review-sourced videos), and edit history (2 review submissions on delivered projects). He is ACTIVE, has a portalToken, has a clean 4v/1s/4h Accelerator allowance with no manual override to preserve, and his 5 months are small enough to read end to end. Bernadette Rabel, Marcee McMullen and John Collins each fail the edit-history axis outright (0 review submissions), and two of the three carry the 5v manual override, which makes them poor allowance baselines. Erica Walker has the most volume (13 months, 215 topics, 52 scripts, 36 videos) and the only MATCHED ProgramCallRecord, but no strategy at all and a 103-topic import artefact on 2026-05 — use her to prove reads scale and to prove A11, not as the reference. Sarina Spinelli is the ONLY source of a genuine round-2 revision and a released-to-client video; copy that one cut chain as a labelled fixture rather than adopting her ENDED 2-month enrollment.\n\nWHAT PREVIOUS CONTENT MUST CONTAIN: 162 ContentVideo rows keyed before 2026-09, over months 2025-04 through 2026-08, against 95 pre-September ContentMonths (80 of them historical=true / status IMPORTED). All 162 carry a derived filming date and 160 are DELIVERED with no client approval record, so every one of them needs F15's legacy download exemption and none of them may enter a new approval clock. Current-month work is only 6 videos (1 DELIVERED, 4 in EDITING/CLIENT_REVIEW). Do not fabricate months: 67 of 110 ContentMonths have no videos at all, and 27 of the 43 that do disagree with videosOwed in one direction or the other.\n\nORDER OF REPAIR, by measured blast radius: (1) stabilise video identity before exposing anything — 157 library rows hang off a filtered Aryeo array index, and re-keying must happen before clients can see them; (2) build the Script/Topic -> Video join, which is the single empty edge in §15's chain and gates topic naming, per-video release and captions; (3) replace the derived filmedAt with photographer confirmation from the upload flow (F12), since 100% of dates are currently inferred; (4) re-anchor the promise clock off Project.shootDate onto the appointment end (F27), and only then decide the client-facing milestone — nothing has been promised to a client in the hub, so that decision is unconstrained.\n\nTEST ACCOUNT PLAN, safe today: `npx tsx scripts/create-test-client.ts \"Jordan Spackman TEST\" jordan 2026-09 --scenarios` passes both existing guards unchanged and needs no weakening of testClients.ts. Copy Joe Sutow's strategy text, one month's topics and one script chain as labelled fixtures with fresh ids; take Sarina's round-2 cut shape as the revision fixture. Two things must be settled before it can exercise anything external: Jordan confirming one test email and one test mobile (the database offers three conflicting candidates), and a sandbox or mock adapter for Aryeo, because no guard currently stands between a TEST client and a provider write.\n\nPROBES: seven read-only scripts left in place at /Users/jordanspackman/Realtour Pilot POT Dashboard/scripts/_recon/ — cp-inventory.ts, cp-candidates.ts, cp-linkage.ts, cp-timeline.ts, cp-strategy.ts, cp-projects.ts, cp-questionable.ts. All are pure SELECT/count/groupBy against prisma with no helper that rebuilds a cache; none writes. I modified no source file and touched no tracked file; the 13 files showing as modified in git status are the other agents' comms-coaching work, untouched by me. HEAD is still 8e1f153."

### backup-and-safety

**Verified:**

- DATABASE IDENTITY (ran `SELECT version()`, pg_settings, pg_tables against the live DB, read-only raw SQL only): PostgreSQL 18.6 on Neon. project_id `cool-sun-51091200`, branch `br-polished-bar-ai5lbq8s`, endpoint `ep-misty-mountain-aik40uah`, timeline `82ed68f8f7356f4ba34fb83df83a7f18`, compute_mode=primary, compute id `compute-small-resonance-ainw5oa0`. Database `neondb`, 145 MB, 117 tables in `public`, 108,511 rows total, 99 non-empty. Connected role is `neondb_owner` — rolsuper=false, rolcreatedb=TRUE, rolcreaterole=TRUE. The host in DATABASE_URL has no `-pooler` suffix, so this is a direct (non-PgBouncer) connection, which is what `prisma db push` needs.
- BACKUP MECHANISM EXISTS AND IS SCHEMA-DERIVED. `/Users/jordanspackman/Realtour Pilot POT Dashboard/scripts/backup-content-program.ts` dumps 56 named models to one JSON file, stamped with `takenAt`, git `commit`, and a sha256 prefix of prisma/schema.prisma. A model that cannot be read is a hard failure (exit 1, nothing written), not a silent skip. Recovery path: `scripts/restore-content-program.ts` (--dry-run / --deep rolled-back rehearsal / --apply, with a `--strict` dangling-ref refusal) and `scripts/recovery-drill.ts` (three scenarios against isolated PGlite instances).
- COVERAGE AGAINST THE CURRENT SCHEMA: the backup covers 56 of 117 models. I extracted the MODELS array and diffed it against `git show HEAD:prisma/schema.prisma`: every one of the 56 resolves to a real model (no stale names). The 61 uncovered models are the operational core — Project, Deliverable, DeliverableOutput, ReviewSubmission, Appointment, AppUser, TeamMember, SmartTask, Notification, NotificationDelivery, OutboxMessage, CommLog, UploadedFile, QcRecord, TopazJob, Activity, AppSetting, WebhookEvent, CronRun, and the finance/payroll/Plaid/QBO set.
- DEPENDENCY LEAKAGE, MEASURED: 27 reference edges leave the backed-up set into 8 uncovered models — Project (8 edges, 1 Postgres-enforced), ReviewSubmission (7, 0 enforced), AppUser (4, 0), SmartTask (3, 0), OutboxMessage (2, 0), DeliverableOutput (1, 1 enforced), Deliverable (1, 0), TeamMember (1, 0). Live row counts in those targets: Project 1585, Deliverable 4046, DeliverableOutput 924, SmartTask 1933, ReviewSubmission 42, OutboxMessage 54, TeamMember 9, AppUser 7.
- RESTORE EXERCISED AWAY FROM LIVE RECORDS (I ran it): `npx tsx scripts/recovery-drill.ts /Users/jordanspackman/Downloads/rtp-backup-before-wf02-schema.json --port 5533`, with DATABASE_URL unset in the parent so no production URL could leak. Result: DRILL PASSED. Scenario 1 rebuilt 6,409 of 6,426 rows into an empty PGlite database with `prisma db push` of the CURRENT working-tree schema — 164 scripts' currentVersion pointers resolved, 0 approved-version pointers dangling, 0 topics pointing at a missing pillar. Scenario 2 correctly REFUSED to write 29 orphan enrollments into a database with no Client rows. Scenario 3 restored 362 clients + 29 enrollments with 0 unresolved parentClientId self-references. Production was never touched; the only production data read was the backup file already on disk.
- THE 17 ROWS THAT DID NOT RESTORE are all RevisionBrief, blocked by `RevisionBrief_projectId_fkey` — a real Postgres foreign key into `Project`, which the backup does not contain. This is the concrete proof that the content-program backup cannot rebuild itself bare-metal. 21 RevisionBrief rows exist live today.
- LAST BACKUP AND WHERE IT LIVES: two files, both in /Users/jordanspackman/Downloads/ (outside the repo, as intended). `rtp-content-program-backup-2026-09-17.json` takenAt 2026-09-18T00:19:26.891Z at commit b1f0d8e6; `rtp-backup-before-wf02-schema.json` takenAt 2026-09-18T00:44:06.723Z at commit d5a79f19. Both 10.84 MB, both 56 models / 6,426 rows, both schemaHash `3fa8b156600ddb33`. The newest is 3 days 20 hours old as of 2026-09-21 20:35 UTC. A stray temp file `rtp-content-program-backup-2026-09-17.json.parents.json` (1.0 MB) is left over from a drill subset.
- THE EXISTING BACKUP IS STALE AGAINST BOTH THE CODE AND THE DATABASE. Its schemaHash is `3fa8b156600ddb33`; HEAD's prisma/schema.prisma hashes to `f29fe6579baac031` and the working tree to `d2c6bc8b20a90aaf`. A restore would print the 'replaying across a migration' warning. Drift in the data since it was taken is small and additive: +37 rows across 12 models (Client 362->363, ProgramTranscriptSource 0->10, PortalVisit 2155->2165, RevisionBrief 17->21, ProgramCallRecord 1->3, ProgramCalendlyEventMapping 0->2, ContentVideoSource 176->179, ContentVideo/PortalVideo/ProgramEnrollmentChange/ClientEmailAlias/ProgramTranscriptJob +1 each).
- THE LIVE SCHEMA IS AHEAD OF COMMITTED HEAD. I queried information_schema: `CommLog.senderUserId` and `CommLog.senderTeamMemberId` EXIST in production. `git show HEAD:prisma/schema.prisma` does not declare them (verified false). So a `prisma db push` for the in-flight comms-attribution work has already been applied to production, and it happened after the last backup was taken. This is state, not a criticism of in-progress work — but it means any backup taken now carries a schema stamp that matches neither of the two files on disk, and the 'fresh backup before schema changes' rule has one change already live that predates the next backup.
- NO pg_dump. `which pg_dump` and `which psql` both return not found; no postgresql/libpq formula under /opt/homebrew/opt. The gold-standard full dump the backup script's own header calls for has never been takeable on this machine. PGlite IS installed (@electric-sql/pglite + pglite-socket in node_modules), which is why the recovery drill works offline.
- NO AUTOMATED BACKUP. vercel.json declares 12 cron entries (gmail */5, topaz */5, sync hourly, reconcile :30, daily 08:00, daily-clients 08:20, daily-reconcile 08:40, evening at 23/00/02/03). None of them backs anything up; `grep -rn backup src/app/api/cron/` returns one unrelated comment. Backup is a manual, human-initiated act.
- PRODUCTION-WRITE GUARDS THAT EXIST AND WORK: (1) `scripts/guard-prod-db.ts` refuses when DATABASE_URL matches /neon\.tech|vercel|amazonaws/ unless I_UNDERSTAND_THIS_WIPES_PROD=yes — wired into `npm run db:reset`. (2) `prisma/seed.ts` and `prisma/seed-clean.ts` each carry the SAME guard inline, reading .env directly because tsx does not autoload it. So AGENTS.md's claim that 'both now refuse hosted URLs unless forced' is accurate — `db:seed` is not routed through guard-prod-db.ts but self-guards. (3) `src/lib/testClients.ts`: `isTestClientName` = /\btest\b|\bjohn doe\b/i, `isStaffControlledEmail` = /^[^@\s]+@realtourpilot\.com$/i, `assertTestClient` throws NotATestClientError. (4) `portalAccess.inviteClientUser` refuses a non-TEST client and a non-staff email while the `portal_invites` switch is off. (5) `programReminders` policy `testClientsOnly: true` by default. (6) `scriptShare` suppresses with reason `launch_not_authorised` for non-TEST clients. (7) The outbox writes its ledger row BEFORE the provider call and holds the dedupeKey forever on an `unknown` outcome.
- ALL CLIENT-FACING PROGRAM AUTOMATION IS OFF AT THE DATABASE. `SELECT * FROM "ProgramAutomation"` returns ZERO rows, and `src/lib/programAutomation.ts` is explicit that a missing row is DISABLED with no code default that turns anything on. All 13 switches are therefore off: reminders, transcript_jobs, ai_runs, publishing, script_share_email, portal_invites, portal_login_email, topic_refresh, strategy_generation, session_booking, cut_transcripts, caption_assistant, fact_extraction.
- WHAT HAS ACTUALLY BEEN SENT: OutboxMessage holds 54 rows, ALL in state `accepted` — zero failed, zero unknown. ProgramReminder holds 8 rows; the ones I read went to `info+caratest@realtourpilot.com` and `info+bobbytest@realtourpilot.com` on 2026-09-17 with real Gmail providerMessageIds (e.g. 1a0afe0c3765e306), requestedBy `w3-verifier`. The pre-launch gate held: no real client address appears.
- F28 IS CORRECT AND I CONFIRMED THE MECHANISM: `isTestClientName` requires a word-bounded 'test' or 'john doe'. The string 'Jordan Spackman' fails it. The test account must literally be named e.g. 'Jordan Spackman TEST'.
- TWO 'Jordan Spackman' CLIENT ROWS ALREADY EXIST AND NEITHER IS SYNTHETIC: `cmqikskt1008u9k9qej9ltjy5` (jspackman215@gmail.com, phone +12678279038, 9 real Projects) and `cmtl9n46u0001la04skvn78nb` (info@realtorpilot.com — note the misspelled domain, 'realtorpilot' not 'realtourpilot' — phone +1267-827-9038). Both have autoConfirmationText=true and autoDeliveryText=true. Neither may be reused or renamed for the test account.
- JORDAN'S VERIFIED INTERNAL IDENTITIES: TeamMember `cmqidx2on00009ke2vvuyx40p` name 'Jordan Spackman', email info@realtourpilot.com, phone +12155348650, role PHOTOGRAPHER. AppUser info@realtourpilot.com = 'Jordan Spackman', role OWNER. Full AppUser roster is 7 people: harrisonwells457@gmail.com (PHOTOGRAPHER), hello@realtourpilot.com Kyle Smith (ADMIN), info@realtourpilot.com Jordan (OWNER), james@realtourpilot.com (ADMIN), john@realtourpilot.com (EDITOR), jordan.laurenspackman@gmail.com Lauren Spackman (OWNER), kim@realtourpilot.com (EDITOR).
- THE EXISTING 'Bobby TEST Michael TEST' FIXTURE IS NOT ISOLATED. Client `cmtl98xl90008jl04yt5zawnv` carries email bobmike0214@gmail.com (NOT staff-controlled) and phone +12678279038, with autoConfirmationText=true and autoDeliveryText=true, and owns 2 real Project rows. 'Cara TEST' is clean by comparison (phone null, info+caratest@realtourpilot.com) but also owns 2 Projects.
- ENROLLMENT POPULATION: 29 ContentEnrollment rows — 13 ACTIVE (Arielle Roemer, Ashley Brunner, Bernadette Rabel, Bobby TEST, Cara TEST, Erica Walker, Joe Sutow, John Collins, Kristin Ciarmella, Marcee McMullen, Mike Ciunci, Mike Flatley, Rick Schultz), 13 ENDED, 3 PAUSED (Alex Ercole Stackhouse, Matthew Hutton [the only Pro], Tony Cedrone). Portal tokens are issued on 12 of the 13 ACTIVE (Mike Flatley has none) plus Sarina Spinelli (ENDED).
- REFERENTIAL INTEGRITY IS MOSTLY UNENFORCED: the schema documents 174 plain refs (`field String // -> Model`) across 43 models, while the database holds only 49 FOREIGN KEY constraints in `public`. The restore script's `--strict` dangling-ref pre-flight is the only thing standing in for the missing ~78%.
- §15 EXTENSION POINTS ARE GENUINELY ADDITIVE. I read information_schema for every model in the spec's §15 table. Already present and needing nothing: ContentMonth (30 cols, monthKey/videosOwed/status all there), ProgramOnboarding (23 cols incl. assetChecklistJson, discoveryCallRecordId, strategyApprovedVersionId), ProgramSessionRequest (34 cols incl. slotStart, slotEnd, locationText, aryeoAppointmentId, bookingState, capacityCheckJson, dedupeKey), ClientAsset (14) + ClientAssetVersion (16, incl. fileRef/valueText/valueJson/contentHash — structured fonts/music/links fit without new columns), ContentVideo (33, incl. topicId/selectionId/scriptVersionId/outputId/countsTowardAllowance), ClientDecision (24, incl. round/receiptState/supersededById/dedupeKey), PortalComment (17, incl. videoId/timeSec/resolvedBy). Genuinely missing and needing new NULLABLE columns only: ContentEnrollment + stripePriceId, stripeProductId, stripeSubscriptionId; ProgramSignup + stripeSessionId, stripePriceId, welcomeSentAt; ContentTopic + dismissedAt, dismissedBy, dismissReason; ContentInterview + readyAt; ContentScriptVersion + clientApprovedAt, clientApprovedBy. Twelve nullable columns in five tables. No drops, no renames, no type changes, no new required columns — additive by inspection.
- THE DEPLOYED APP RESPONDS: `curl -L https://hub.realtourpilot.com/` returns 200 in 0.83s, served by Vercel (x-vercel-id iad1::t994f-1790022919046-745bbac8c8cf) at 2026-09-21 20:35 UTC.
- VERCEL PRODUCTION ENV holds 18 variables (DATABASE_URL, APP_SECRET, AUTH_ENFORCE, CRON_SECRET, GOOGLE_*, DROPBOX_*, SLACK_SIGNING_SECRET, SCRIPTING_*, QBO_*, FRAMEIO_CLIENT_ID, GOOGLE_MAPS_API_KEY, BLOB_READ_WRITE_TOKEN, NEXT_PUBLIC_APP_URL). There is NO NEON_API_KEY, no POSTGRES_* and no Neon-integration variable — DATABASE_URL was pasted by hand, so this project is not wired to Vercel's Neon integration.

**Blockers:**

- NEON BRANCH FEASIBILITY CANNOT BE ESTABLISHED WITHOUT A CREDENTIAL. Operation: create a scratch branch of `br-polished-bar-ai5lbq8s` in project `cool-sun-51091200` and restore into it. Verified limitation: no NEON_API_KEY exists in .env or in Vercel production env, and neonctl is not installed. Smallest next action for Jordan — open console.neon.tech, project 'cool-sun-51091200', and read three things off the Branches tab: the plan name, the history-retention window, and whether a 'Create branch' button is offered. If he wants me to do it, a read-only Neon API key pasted into .env as NEON_API_KEY is enough for me to enumerate branches and quotas without creating anything. EXACT STEPS once available, none of which I will take unsold: (1) Neon console -> Branches -> Create branch from `production` at 'now' (or at a chosen LSN/timestamp); (2) add a compute endpoint to it; (3) copy its connection string; (4) run `scripts/restore-content-program.ts <backup.json> --deep --strict` against THAT url with DATABASE_URL set explicitly in the child env; (5) delete the branch. Cost is the branch's own compute-hours plus the storage delta — negligible for 145 MB, but confirm against the plan before creating it.
- A FULL pg_dump IS NOT POSSIBLE ON THIS MACHINE RIGHT NOW. Operation: capture the 61 operational models the content-program backup does not cover. Verified limitation: `which pg_dump` -> not found; no postgresql client formula installed. Smallest next action: `brew install libpq` and use `/opt/homebrew/opt/libpq/bin/pg_dump` (or `brew install postgresql@18` to match the server's 18.6 — a version-16 pg_dump will refuse an 18.6 server). Then `pg_dump --no-owner --no-acl -Fc "$DATABASE_URL" -f ~/rtp-full-<date>.dump`. This is a pure read of production and is safe, but it is a software install so I am not doing it unasked. Alternative with zero install: extend scripts/backup-content-program.ts's MODELS array to all 117 models — same JSON mechanism, proven restore path, and it would close the RevisionBrief/Project gap the drill exposed.
- I CANNOT DEMONSTRATE A RESTORE INTO NEON WITHOUT A WRITE. The PGlite drill proves the payload is replayable against the current schema, which is the substantive question, but it does not prove it against Neon's own Postgres 18.6 build. Closing that needs either the scratch branch above or a second database on this endpoint (rolcreatedb=true would allow it). Neither is a thing I will do in a read-only phase.

**Spec corrections:**

- THE MEMORY NOTE 'a previous audit found a backup covering 9 of 56 models' IS NOW STALE AND SHOULD NOT BE CARRIED FORWARD AS A LIVE FINDING. It was fixed on Sep 17–18. Today the backup enumerates 56 models derived from the schema, fails hard on any model it cannot read, and stamps the file with commit + schema hash. The correct statement of the CURRENT gap is different and narrower: the backup covers 56 of 117 models, and the 61 it omits are the operational core, not content-program tables.
- THE SPEC'S §1.4 PHRASE 'verify backup coverage against the current schema' IS ALREADY PARTLY SATISFIED BY EXISTING TOOLING, and the audit does not credit it. scripts/restore-content-program.ts compares the file's schemaHash to the live one and announces a cross-migration replay; its --dry-run compares every backed-up column against a live sample row; its --deep mode performs every upsert inside an always-rolled-back transaction. Jordan's requirement is met by running these, not by building anything.
- THE SPEC SAYS NOTHING ABOUT THE ONE REAL COVERAGE DEFECT, WHICH THE DRILL FOUND EMPIRICALLY: 17 RevisionBrief rows cannot be restored into an empty database because `RevisionBrief_projectId_fkey` points into `Project`, which the backup does not contain. This is the single concrete, reproducible hole in the current backup, and it deserves to be in the checklist ahead of anything speculative.
- F28 IS CONFIRMED, NOT OVERSTATED — and the underlying risk is worse than the spec describes. The spec says only that 'Jordan Spackman alone is not considered a test record by the current guard'. Also true, and unmentioned: TWO real Client rows are already named exactly 'Jordan Spackman' (one owning 9 production Projects, one on the misspelled domain info@realtorpilot.com), so a name-based test guard would sit next to two live look-alikes. And the existing 'Bobby TEST Michael TEST' fixture — which the guard DOES accept — carries a real gmail address, a real phone, both auto-text flags on, and 2 Projects, so the 'existing test-name safeguards' the spec tells us to reuse do not currently deliver the isolation §16 asks for.
- AGENTS.md's 'Safe: npm run db:push' NEEDS A QUALIFIER. It is safe for additive diffs only. It has no guard of its own — unlike db:reset and both seeds, which self-refuse on hosted URLs — and it will remove a column the schema stops declaring. For this program that matters, because §15 is a list of schema changes and the one already applied to production (CommLog's two columns) went in with no backup taken before or since.

**Recommendation:** "BEFORE ANY SCHEMA CHANGE, in this order.\n\n1. Take a fresh backup NOW and keep it outside the repo: `npx tsx scripts/backup-content-program.ts ~/rtp-backup-<ISO date>-pre-content-program.json`. It will stamp schemaHash d2c6bc8b20a90aaf (or whatever the tree hashes to at that moment) and should show ~6,463 rows across 56 models. Delete the stray `rtp-content-program-backup-2026-09-17.json.parents.json` drill leftover while you are there.\n\n2. Prove it before trusting it: `env -u DATABASE_URL npx tsx scripts/recovery-drill.ts <that file> --port 5533`. It runs entirely on PGlite, takes a few minutes, touches nothing remote, and gives Jordan the 'exercised restoration away from live records' evidence §1.4 asks for. Expect the same 17 RevisionBrief failures until item 3 is done.\n\n3. Close the one real coverage hole. Cheapest fix with no install: add the 8 leaked targets to the MODELS array in dependency order — Project and DeliverableOutput first (they are the two Postgres-enforced edges), then ReviewSubmission, Deliverable, AppUser, TeamMember, SmartTask, OutboxMessage. That converts 27 unbacked edges into 0 and should make the drill restore 6,426 of 6,426. Better still, and only marginally more work: back up all 117 models and stop reasoning about which subset matters. The proper belt-and-braces answer is `brew install libpq` + a `pg_dump -Fc`, which needs Jordan's go-ahead for the install.\n\n4. All twelve §15 columns are additive — three on ContentEnrollment, three on ProgramSignup, three on ContentTopic, one on ContentInterview, two on ContentScriptVersion, every one of them nullable. Push them in ONE `prisma db push`, not five, so there is one schema stamp to reason about, and take the backup in step 1 immediately before it. Read `prisma db push`'s plan output before confirming; the command has no guard of its own and there is nothing between it and production.\n\nTHE JORDAN TEST ACCOUNT — a safe plan given what is actually there.\n\nName it `Jordan Spackman TEST`. The word-bounded guard then accepts it, and the two existing real 'Jordan Spackman' client rows (cmqikskt1008u9k9qej9ltjy5 with 9 live Projects, and cmtl9n46u0001la04skvn78nb on the misspelled info@realtorpilot.com) must be left untouched — do not rename, merge or reuse either. Sign-in address `info+jordantest@realtourpilot.com`, which is what isStaffControlledEmail requires and what Jordan already controls.\n\nLeave `Client.phone` NULL on it, and set autoConfirmationText and autoDeliveryText to false. This is the important one: clientTextSweeps.ts has no test-client gate, is not one of the 13 ProgramAutomation switches, and therefore is not off. A test client with a phone and those flags on is one shoot confirmation away from a real text. The safest test account is one with no SMS route at all; if a text path genuinely needs exercising, use Jordan's TeamMember number +12155348650 only after he confirms it, and never the 267 number that currently sits on three different client rows and that nobody here has established the owner of.\n\nReuse `scripts/create-test-client.ts` rather than writing a new seeder — it already calls assertTestClient first, refuses non-staff addresses, is idempotent, and its `--scenarios` mode stands up the topic bank, an insufficient interview, three differently-scoped facts and a post-approval refresh run. Extend it for the §16 scenarios it does not yet cover rather than starting a second one.\n\nKeep every ProgramAutomation switch off. Zero rows exist and a missing row is OFF with no code default that flips it, so the whole program is currently inert by construction — that is the strongest safety property this system has and it should be the last thing changed, one switch at a time, after Jordan sees the test walkthrough.\n\nONE THING TO FIX WHILE BUILDING, because it will otherwise bite. There is no single outbound chokepoint: outbox.ts calls the provider and knows nothing about test clients, and every pre-launch protection is a per-feature check a new send path can simply forget. Put the recipient check inside `sendThroughOutbox` — while any launch gate is unauthorised, refuse a recipient that is neither staff-controlled nor on an explicit allowlist — so that correctness stops depending on each future author remembering. The per-feature gates can stay; this is the floor beneath them.\n\nFINALLY, tell Jordan the one thing only he can unblock: three facts off the Neon console (plan name, history-retention window, whether branch creation is offered for project cool-sun-51091200). That decides whether restore rehearsals can run against real Neon or must stay on PGlite. Everything else in this phase can proceed without him."
